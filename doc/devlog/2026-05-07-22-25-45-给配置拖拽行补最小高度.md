# 给配置拖拽行补最小高度

## 本次目标

- 将请求配置区拖拽边界的最小高度落到 `RowDefinition`，避免配置区被拖到低于可用操作高度。

## 核心改动

- 为 HTTP 接口工作台和快捷请求工作台的配置区行增加 `MinHeight="118"`。
- 更新工作台布局结构测试，确认配置区行同时具备最小高度和动态最大高度。

## 修改文件

- `src/ApixPress.App/Views/Controls/HttpInterfaceWorkbenchView.axaml`
- `src/ApixPress.App/Views/Controls/QuickRequestWorkbenchView.axaml`
- `tests/ApixPress.App.Tests/Views/WorkbenchLayoutTests.cs`
- `TASK.md`

## 校验情况

- 默认测试输出目录被正在运行的 `ApixPress.exe` 锁定，因此改用独立输出目录验证。
- 已执行：`dotnet test tests\ApixPress.App.Tests\ApixPress.App.Tests.csproj --filter FullyQualifiedName~WorkbenchLayoutTests --no-restore -o artifacts\test-verify\T148-layout-output`
  - 结果：2 个测试全部通过。
- 已执行：`dotnet test tests\ApixPress.App.Tests\ApixPress.App.Tests.csproj --no-restore -o artifacts\test-verify\T148-all-output`
  - 结果：162 个测试全部通过。
- 已执行：`dotnet build src\ApixPress.App\Apifox.App.csproj -o artifacts\build-verify\T148-app --no-restore`
  - 结果：构建成功，0 警告，0 错误。

## 风险或遗留项

- 未关闭用户当前运行中的应用实例；未做人工拖拽 smoke test。
