# 本次目标

继续减少项目工作区左侧目录树的重复刷新开销，避免快捷请求或接口相关操作总是一起触发整块导航重建。

# 核心改动

- 将工作区导航刷新拆分为“接口树”和“快捷请求树”两个作用域，不再默认同时重建两侧列表。
- 根据 `RequestCases` 集合变更内容判断刷新范围：快捷请求变化只刷新快捷请求树，接口/用例变化只刷新接口树，整批加载时才全量重建。
- 拆分 `ProjectWorkspaceTreeBuilder` 的接口树与快捷请求构建逻辑，减少不必要的树构造与集合替换。
- 补充回归测试，验证保存快捷请求时不会替换现有接口树节点引用。

# 修改文件

- `src/ApixPress.App/ViewModels/ProjectWorkspaceTreeBuilder.cs`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.Persistence.cs`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`
- `TASK.md`

# 校验情况

- 已执行：`dotnet test Apifox.slnx --no-restore`
- 结果：54 个测试全部通过

# 风险或遗留项

- 当前接口树内部仍是按“整棵接口树”重建，而不是对单个目录、接口、用例做真正的局部 diff；相比之前已经缩小刷新范围，但还存在进一步优化空间。
