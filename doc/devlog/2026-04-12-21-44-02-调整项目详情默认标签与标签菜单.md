# 2026-04-12 21:44:02 调整项目详情默认标签与标签菜单

## 本次变更
- 项目详情默认打开时，不再先显示 `新建...` 落地页，而是直接进入新建 HTTP 接口编辑页。
- 工作区标签栏将 `+` 从标签列表内部移出，并新增 `...` 菜单入口。
- `...` 菜单中补充 `关闭全部标签页`、`关闭当前标签页`、`关闭其它标签页` 三个操作。

## 核心实现
- 在 `ProjectTabViewModel` 中增加标签菜单开关状态和三个关闭标签命令。
- 调整默认空工作区的创建逻辑，首次进入项目详情或关闭到空状态时默认补一个 HTTP 接口标签。
- 在 `MainWindow.axaml` 中重构标签栏顶部布局，把 `+ / ...` 调整为独立工具位，并补充下拉菜单面板。
- 在 `Global.axaml` 中补充标签更多按钮和菜单卡片样式。

## 验证
- 已执行：`dotnet build Apifox.slnx -p:UseAppHost=false`
- 已执行：`dotnet test Apifox.slnx -p:UseAppHost=false --no-build`
- 已执行：`dotnet publish Apifox.slnx -p:UseAppHost=false`
