# 收紧生成代码弹窗布局

## 本次目标

- 参考目标图，进一步把生成代码弹窗整体收紧。
- 让头部、左侧语言栏、右侧标题、页签和代码区更接近紧凑型代码生成器布局。

## 核心改动

- 收紧生成代码弹窗整体宽高与内边距，缩短头部高度。
- 移除标题下方冗余副标题堆叠，保留更紧凑的标题区。
- 缩窄左侧语言栏宽度，减少语言项间距与内边距，并把 `Shell(3)`、`C#(1)` 直接放进语言项里。
- 将右侧代码区域包进更紧凑的编辑器卡片，缩小页签尺寸和代码框内边距，使布局更贴近参考图。

## 修改文件

- `src/ApixPress.App/Views/Controls/RequestCodeDialogView.axaml`
- `src/ApixPress.App/Assets/Styles/DialogStyles.axaml`
- `TASK.md`

## 校验情况

- 已执行整套测试：`dotnet test .\Apifox.slnx --no-restore`，通过 154 项测试。
- 校验过程中曾因运行中的 `ApixPress.exe` 锁定输出文件导致测试失败，关闭进程后已重新通过。

## 风险或遗留项

- 本次未执行人工界面 smoke test；当前结论基于自动化测试与 XAML/样式回读。
- 当前弹窗已明显更紧凑，但仍沿用仓库现有控件体系；若后续要继续逼近参考图，还可以再补顶部复制图标、代码行号等细节。
