# 本次目标

- 调整快捷请求编辑页上下区域比例，让返回响应区域更大，更接近参考图中的阅读比例。

# 核心改动

- 将快捷请求编辑区主内容由“上方自适应 + 下方剩余”改为固定比例分配，高度按 `4:3` 分给请求配置区和响应区。
- 提高响应区域卡片的最小高度，避免返回结果内容区过小。
- 同时为请求配置卡片补最小高度，保证上下布局比例更稳定。

# 修改文件

- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 未执行：桌面端手工 smoke test，当前会话未直接打开窗口核对新比例视觉效果

# 风险或遗留项

- 本次是快捷请求编辑页整体比例调整；如果后续希望再进一步贴近参考图 2，还可以继续压缩上方参数区留白或引入拖拽分割条。
