# 本次目标
- 将 `ApixPress` 主程序与更新器的发布链路收口为单 exe 产物
- 保留默认配置与数据库初始化能力，同时避免发布目录继续依赖外置 `appsettings.json` 和迁移脚本

# 核心改动
- 为 `src/ApixPress.App/Apifox.App.csproj` 与 `src/ApixPress.Updater/ApixPress.Updater.csproj` 补充 Release 单文件发布配置：`PublishSingleFile`、`IncludeNativeLibrariesForSelfExtract`、`PublishReadyToRun`、`EnableCompressionInSingleFile`
- 将主程序 `appsettings.json` 与 `Data/Migrations/*.sql` 改为内嵌资源，不再默认复制到发布目录
- 在 `ServiceBootstrapper` 中改为“内嵌默认配置 + 外部 `appsettings.json` 可选覆盖”
- 在 `DatabaseInitializer` 中增加迁移脚本内嵌兜底，外部脚本不存在时从程序集资源读取
- 更新 GitHub Actions 发布脚本与 README，显式说明单 exe 发布策略

# 修改文件
- `src/ApixPress.App/Apifox.App.csproj`
- `src/ApixPress.App/ServiceBootstrapper.cs`
- `src/ApixPress.App/Data/Context/DatabaseInitializer.cs`
- `src/ApixPress.App/Helpers/EmbeddedResourceReader.cs`
- `src/ApixPress.Updater/ApixPress.Updater.csproj`
- `.github/workflows/release.yml`
- `README.md`
- `TASK.md`
- `doc/devlog/2026-04-17-22-45-20-发布链路收口为单exe.md`

# 校验情况
- 已执行：`dotnet publish src\ApixPress.App\Apifox.App.csproj -c Release -r win-x64 --self-contained true -o .artifacts\publish-single\app -p:PublishAot=false -p:PublishTrimmed=false -p:DebugType=None -p:DebugSymbols=false -p:IncludeNativeLibrariesForSelfExtract=true -v minimal`
- 已执行：`dotnet publish src\ApixPress.Updater\ApixPress.Updater.csproj -c Release -r win-x64 --self-contained true -o .artifacts\publish-single\updater -p:PublishAot=false -p:PublishTrimmed=false -p:DebugType=None -p:DebugSymbols=false -p:IncludeNativeLibrariesForSelfExtract=true -v minimal`
- 已执行：启动 `.artifacts\publish-single\app\Apifox.App.exe`，进程 8 秒内保持存活
- 已执行：启动 `.artifacts\publish-single\updater\ApixPress.Updater.exe`，进程正常退出，退出码为 `0`
- 验证结果：主程序发布目录仅保留 `Apifox.App.exe`，更新器发布目录仅保留 `ApixPress.Updater.exe`

# 风险或遗留项
- 当前单文件方案依赖运行时将原生依赖解压到临时目录，不属于纯内存加载
- 本次仅完成发布与启动 smoke test，未继续执行完整业务功能回归或自动化测试
- 发布总包仍会包含主程序 exe 与更新器 exe 两个文件，这是当前自动更新链路所需结构，并非单个包内只有一个文件
