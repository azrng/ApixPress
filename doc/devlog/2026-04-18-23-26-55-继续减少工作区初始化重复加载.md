# 本次目标

继续排查项目工作区打开与刷新链路中的重复加载，减少初始化阶段不必要的数据库读取与目录重建。

# 核心改动

- `ProjectTabViewModel.LoadWorkspaceAsync` 去掉了与 Swagger 导入同步链路重复的已保存请求预加载，避免项目打开时对用例列表执行两次全量查询。
- 将请求历史加载与导入文档刷新改为并行执行，缩短工作区初始化的串行等待时间。
- 补充回归测试，锁定“初始化阶段只加载一次请求用例”的行为，避免后续改动把重复加载带回来。

# 修改文件

- `src/ApixPress.App/ViewModels/ProjectTabViewModel.WorkspaceActions.cs`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`
- `tests/ApixPress.App.Tests/ViewModels/ViewModelSharedTestDoubles.cs`
- `TASK.md`

# 校验情况

- 已执行：`dotnet test Apifox.slnx --no-restore`
- 结果：53 个测试全部通过

# 风险或遗留项

- 当前工作区初始化仍会在导入文档刷新后重建一次左侧目录树，已经去掉重复加载，但尚未做到真正的局部树 diff 更新。
