# 本次目标
- 将 Swagger 导入弹窗中的“上传文件 / URL 导入”切换区域改为更清晰的 tab 样式。

# 核心改动
- 将导入弹窗中的模式切换区域从原有按钮布局调整为 tab strip 容器。
- 新增 `ProjectImportTabStrip` 和 `ProjectImportModeTab` 样式，让选中项以底边高亮方式展示，未选中项保持普通页签视觉。
- 保持原有命令和 `IsImportFileMode` / `IsImportUrlMode` 绑定不变，不影响导入逻辑。

# 修改文件
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`

# 校验情况
- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 已执行：`dotnet test Apifox.slnx -c Release -p:UseAppHost=false`

# 风险或遗留项
- 当前已完成编译和测试验证，但 tab 的最终视觉细节仍建议结合真实界面确认一次间距和选中态是否完全符合预期。
