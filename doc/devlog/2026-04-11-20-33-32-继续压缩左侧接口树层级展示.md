# 继续压缩左侧接口树层级展示

## 本次目标
- 继续把项目详情左侧接口树调整得更接近参考图的层级结构和区块表达。

## 核心改动
- 将“默认模块”从树节点中抽出来，改成左侧区块标题，树里只保留 `接口 → 分组目录 → 接口 → 用例`。
- 将“快捷请求”改成区块标题 + 直接列表，不再额外包裹一层“快捷请求”根节点。
- 补充左侧区块标题样式和空状态文案，让左栏结构更接近目标图。

## 修改文件
- `TASK.md`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`

## 校验情况
- 已执行：`dotnet build src\ApixPress.App\Apifox.App.csproj`

## 风险或遗留项
- 当前已经去掉重复根节点，但树缩进、箭头位置和图标细节仍可继续按截图微调。
