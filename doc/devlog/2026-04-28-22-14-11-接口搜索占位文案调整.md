# 接口搜索占位文案调整

## 本次目标
- 将接口管理左侧搜索框占位文案改回“搜索”。

## 核心改动
- 保留接口名称与 Path 搜索能力，仅调整搜索框 Watermark 文案。

## 修改文件
- `src/ApixPress.App/Views/Controls/ProjectWorkspaceSidebarView.axaml`
- `TASK.md`
- `doc/devlog/2026-04-28-22-14-11-接口搜索占位文案调整.md`

## 校验情况
- 重新读取改动文件，确认中文显示正常。
- `dotnet build Apifox.slnx`：未通过，当前运行中的 `ApixPress (34480)` 锁定了 `bin\Debug\net10.0\ApixPress.exe` / `ApixPress.dll` 输出文件。
- `dotnet build src\ApixPress.App\Apifox.App.csproj -p:BaseOutputPath=.artifacts\build-check\`：通过，0 警告，0 错误。

## 风险或遗留项
- 纯文案调整，无已知代码风险；默认输出目录构建需在关闭当前运行中的 ApixPress 后重试。
