# 本次目标

将默认启动页调整为更接近参考图的项目列表视图，并把新建项目入口改为右侧抽屉。

# 核心改动

- 重构项目浏览页，改为顶部工具条加项目卡片网格的结构。
- 右上角增加项目搜索和“新建项目”按钮，移除页面内嵌创建表单。
- 新增 `CreateProjectDrawerView`，抽屉中仅保留项目名称和备注信息两个输入项。
- 保留项目管理抽屉用于完整管理，浏览页聚焦列表浏览和快速创建。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/MainWindow.axaml.cs`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `src/ApixPress.App/Views/Controls/CreateProjectDrawerView.axaml`
- `src/ApixPress.App/Views/Controls/CreateProjectDrawerView.axaml.cs`

# 校验情况

- 已执行：`dotnet build`
- 已执行：`dotnet test`
- 已执行：启动 smoke test，应用启动 6 秒内保持存活，随后主动结束进程

# 风险或遗留项

- 当前浏览页仍保留了应用顶栏里的“项目管理”按钮，方便继续进入完整项目管理抽屉。
- 若后续需要进一步贴近参考图，可继续补项目导入入口与更细致的卡片视觉样式。
