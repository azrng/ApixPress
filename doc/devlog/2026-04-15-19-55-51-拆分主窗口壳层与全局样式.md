# 2026-04-15 19:55 拆分主窗口壳层与全局样式

## 本次目标
- 继续缩小 `MainWindow.axaml` 的职责范围
- 将超大的 `Global.axaml` 按视觉域拆分为多个样式文件

## 主要改动
- 新增 `MainWindowTitleBarView`，承接主窗口标题栏、标签栏与窗口控制按钮
- 新增 `ProjectWorkspaceView`，承接活动项目工作区内容与资源模板
- 将 `MainWindow.axaml` 收敛为窗口壳层组合文件
- 新增 `ShellStyles.axaml`、`ProjectBrowserStyles.axaml`、`WorkspaceStyles.axaml`、`DialogStyles.axaml`
- 将 `Global.axaml` 改为样式聚合入口，按顺序包含各个样式域文件

## 验证
- 已执行 `dotnet build Apifox.slnx`
- 已执行 `dotnet test Apifox.slnx`

## 备注
- `ProjectWorkspaceView.axaml` 仍然偏大，下一轮建议继续按侧边栏、工作台编辑器、历史响应区、项目设置区进一步拆分
