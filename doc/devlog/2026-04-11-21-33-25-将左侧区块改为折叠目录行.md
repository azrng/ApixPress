# 将左侧区块改为折叠目录行

## 本次改动
- 将 `默认模块` 下的 `接口 / 数据模型 / 组件库` 从普通标题堆叠改为折叠目录行
- 将 `接口` 行与其下方真实目录树拆开，目录树直接显示接口目录分组，不再重复显示 `接口` 根节点
- 将 `快捷请求` 改为独立折叠目录行，并保留其下方请求树和底部新建按钮
- 新增左侧折叠状态属性与样式，包括选中底色、行内箭头、小操作按钮和目录内容缩进

## 影响文件
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`

## 验证
- 已执行 `dotnet build`
- 已执行启动验证，窗口可正常打开

## 说明
- 本次主要针对你给的图 2 / 图 3 收目录结构和视觉层级
- 数据模型和组件库当前先保留折叠行外观，具体内容页后续再接
