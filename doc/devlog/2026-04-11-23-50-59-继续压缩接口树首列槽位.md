# 继续压缩接口树首列槽位

## 本次目标
- 继续缩小接口目录层级首列带来的左侧空白。

## 核心改动
- 将自定义目录模板首列宽度从 `6` 收到 `4`。
- 将递归子节点容器的额外左边距压到 `0`。
- 将展开箭头宽度同步收到 `4`，并为该箭头单独收窄字号到 `8`。

## 修改文件
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`

## 校验情况
- `dotnet build -c Release`：通过
- `dotnet test`：通过（10/10）

## 风险或遗留项
- 当前首列槽位已经压得很紧，如果还想继续往左贴，后续更适合通过减少节点图标尺寸或改图标布局继续优化。
