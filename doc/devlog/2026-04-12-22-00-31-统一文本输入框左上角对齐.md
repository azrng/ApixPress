# 本次目标

- 统一请求编辑页及相关表单中的文本输入框，让输入内容与占位提示从左上角开始显示。

# 核心改动

- 在全局 `TextBox` 样式中补充左对齐与顶部对齐设置。
- 统一 `TextAlignment`、`HorizontalContentAlignment`、`VerticalContentAlignment`，让单行和多行输入框都遵循一致的起始位置。

# 修改文件

- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 未执行：桌面端手工 smoke test，当前会话未直接打开窗口验证所有输入框实际视觉位置

# 风险或遗留项

- 本次是全局 `TextBox` 样式调整，范围覆盖较广；如果后续发现某个单独输入框需要保留居中表现，再对该类单独覆写即可。
