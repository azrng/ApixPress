# 本次目标

- 统一项目工作区顶部右侧环境下拉框与管理按钮的高度和垂直对齐，避免工具栏右侧控件看起来一高一低。

# 核心改动

- 将顶部工作区工具栏改成左右分区布局，让标签区与右侧环境操作区各自独立对齐。
- 为环境下拉框和右侧管理按钮补齐统一的高度与 `VerticalAlignment="Center"`，保证它们在同一基线上展示。
- 保留左侧标签区的裁切与滚动约束，避免右侧环境控件再被标签区挤占时发生错位。

# 修改文件

- `src/ApixPress.App/Views/Controls/ProjectWorkspaceToolbarView.axaml`
- `src/ApixPress.App/Assets/Styles/WorkspaceSidebarStyles.axaml`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 已执行：`dotnet test tests/ApixPress.App.Tests/ApixPress.App.Tests.csproj -c Release --filter "FullyQualifiedName~ProjectTabViewModelTests"`
- 未执行：桌面端手工 smoke test，当前会话未直接打开工作区顶部工具栏确认最终像素级对齐

# 风险或遗留项

- 当前已统一控件高度与垂直对齐；如果后续设计稿还要求更严格的视觉节奏，可能仍需继续微调工具栏内边距与图标字重。
