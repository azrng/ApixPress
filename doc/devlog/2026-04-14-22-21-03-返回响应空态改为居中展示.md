# 2026-04-14 22:21:03 返回响应空态改为居中展示

## 本次变更

- 调整快捷请求和 HTTP 接口两个“返回响应”空态区域的布局。
- 将空态图标与提示文案包裹到填满剩余区域的占位容器中，确保在响应卡片内部水平和垂直居中。
- 为提示文案补充文本居中样式，避免图标和文字视觉中心不一致。

## 涉及文件

- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`

## 验证

- 已执行 `dotnet build`
- 已执行 `dotnet test --no-build`
- `dotnet test` 首次执行因本地 `Apifox.App.pdb` 被 `.NET Host` 占用失败，重试时通过 `--no-build` 绕过文件锁
