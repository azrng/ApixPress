# 本次目标

修复标题栏微调后导致应用无法启动的问题。

# 核心改动

- 定位到 `MainWindow.axaml` 标题栏窗口控制区的 `Margin` 写法触发 Avalonia 运行时 `InvalidCastException`
- 删除不兼容的 `Margin="{StaticResource Space.1},0,0,0"` 设置，恢复主窗口正常初始化

# 修改文件

- `src/ApixPress.App/MainWindow.axaml`
- `TASK.md`
- `doc/devlog/2026-04-11-10-59-21-修复标题栏边距导致的启动异常.md`

# 校验情况

- 已执行 `dotnet run --project src/ApixPress.App/Apifox.App.csproj`
- 已确认 `Apifox.App` 进程成功启动，主窗口标题为 `ApixPress`
- 已执行 `dotnet test tests/ApixPress.App.Tests/ApixPress.App.Tests.csproj --no-build`
- 测试通过：9/9

# 风险或遗留项

- 本次修复只移除了导致运行时类型转换失败的边距写法，不影响此前的标题栏收紧效果
- 若后续还要继续微调标题栏间距，建议优先改 Grid 列宽或样式资源，避免在属性里拼接资源与逗号厚度值
