# 本次目标

继续从长期维护角度收紧 `ProjectTabViewModel`，把初始化、刷新、环境保存、历史载入与 workspace 联动状态编排从父类中移出，继续压缩父类作为组合根的职责密度。

# 核心改动

- 新增 `ProjectTabLifecycleCoordinator`，集中承接项目页初始化、刷新、当前环境保存、历史请求载入，以及环境切换与 workspace 状态联动编排。
- `ProjectTabViewModel` 改为通过 `_lifecycle` 协调对象委托这些生命周期流程，父类只保留子 ViewModel 组合、少量代理属性与 shell 通知出口。
- 删除 `ProjectTabViewModel.WorkspaceActions.cs` 与 `ProjectTabViewModel.WorkspaceState.cs`，将原先散落的生命周期与状态联动逻辑从父类 partial 中移出。
- 本轮完成后，`ProjectTabViewModel` 已收口为单一主文件，不再依赖额外 `partial` 文件承载生命周期逻辑。

# 修改文件

- `src/ApixPress.App/ViewModels/ProjectTabLifecycleCoordinator.cs`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx --no-restore -v minimal`
- 已执行：`dotnet test Apifox.slnx --no-build --no-restore -v minimal`
- 结果：编译通过，57/57 测试通过

# 风险或遗留项

- 当前 `ProjectTabViewModel` 已进一步收口为组合根，但构造函数里仍保留较多子 ViewModel 创建与回调接线代码。
- 下一步若继续治理，更合适的方向是评估这些构造期依赖拼装是否值得提炼为更明确的工厂或组装器，而不是回退到 partial 拆分。
