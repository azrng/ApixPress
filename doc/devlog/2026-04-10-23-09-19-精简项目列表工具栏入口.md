# 本次目标

继续优化项目列表首页，移除左上角无实际作用的“卡片视图 / 项目总览”入口，保持页面结构更直接清晰。

# 核心改动

- 删除项目列表工具栏左侧的无效视图切换入口，仅保留标题和项目数量说明。
- 调整工具栏列结构，让右侧搜索、项目管理、新建项目的操作区保持紧凑对齐。
- 清理全局样式中已不再使用的浏览页分段按钮样式，避免保留无效样式定义。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`

# 校验情况

- 已执行 `dotnet build`，通过。
- 已执行 `dotnet test`，8 个测试全部通过。
- 已执行启动验证，`Apifox.App.exe` 可正常启动并保持运行。

# 风险或遗留项

- 本次仅调整项目列表页工具栏结构与样式清理，未继续处理更大范围的详情页视觉优化。
