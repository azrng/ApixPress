# 本次目标
- 为接口树右键删除增加二次确认，降低误删风险。

# 核心改动
- 将接口树右键菜单中的删除动作改为先打开确认弹层，再执行真实删除。
- 在 `ProjectTabViewModel` 中增加待删除节点状态、删除说明文案、确认/取消命令。
- 在 `MainWindow` 中增加删除确认弹层，并复用现有对话框样式。
- 补充 ViewModel 测试，覆盖“打开删除确认弹层”和“确认后才真正删除”。

# 修改文件
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/ViewModels/MainWindowViewModel.cs`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`
- `TASK.md`

# 校验情况
- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 已执行：`dotnet test Apifox.slnx -c Release -p:UseAppHost=false`

# 风险或遗留项
- 当前确认弹层是全局遮罩式交互，不是跟随右键菜单的浮层；但删除链路更稳定，也更容易继续扩展删除说明和影响提示。
