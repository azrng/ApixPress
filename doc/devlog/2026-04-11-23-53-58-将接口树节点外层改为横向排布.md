# 将接口树节点外层改为横向排布

## 本次目标
- 继续处理接口目录节点前方仍然明显偏大的空白。

## 核心改动
- 将接口树节点外层从 `Grid(ColumnDefinitions=\"4,*\")` 改为纯横向 `StackPanel`。
- 让展开箭头 / 占位和节点内容直接并排，避免 `*` 列测量带来的潜在隐形留白。
- 保持现有点击加载、展开收起和内部节点内容布局不变。

## 修改文件
- `src/ApixPress.App/MainWindow.axaml`
- `TASK.md`

## 校验情况
- `dotnet build -c Release`：通过
- `dotnet test`：通过（10/10）

## 风险或遗留项
- `dotnet test` 过程中存在运行中的 `Apifox.App.exe` 占用 `bin/Debug` 输出文件的复制重试警告，但最终测试通过。
- 如果这次结构调整后仍存在明显空白，下一步应检查截图中的那一行是否来自图标/徽标自身宽度，而不是层级列宽。
