# 本次目标

修正新建 HTTP 接口编辑区中 BaseUrl 不明显的问题，让未保存状态下也能稳定看到当前环境的 BaseUrl。

# 核心改动

- 将 HTTP 接口 URL 区中的 BaseUrl 展示从普通文本改成独立前缀卡片。
- 前缀卡片直接绑定当前编辑器的 BaseUrl 说明文本：有值时显示当前环境 BaseUrl，未配置时明确提示未配置。
- 补充 BaseUrl 前缀卡片和文本样式，让前缀在未保存状态下也有稳定可见性。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`

# 校验情况

- 已执行 `dotnet build Apifox.slnx -p:UseAppHost=false`
- 已执行 `dotnet test Apifox.slnx -p:UseAppHost=false --no-build`
- 已执行轻量启动校验：启动 `Apifox.App` 后进程保持运行，再主动结束进程

# 风险或遗留项

- 当前前缀卡片展示的是当前环境的 BaseUrl 文本，若环境本身未配置 BaseUrl，会显示未配置提示而不是自动补默认值。
- 本次仍未做人工视觉验收，如你继续发图，我可以再把前缀卡片宽度、颜色和截断策略继续收紧。
