# 本次目标

- 修正请求编辑区“返回响应”空态虽然有居中样式，但实际仍贴在左上角的问题。

# 核心改动

- 将 HTTP 接口工作台和快捷请求工作台的响应区容器从 `DockPanel` 改为两行 `Grid`。
- 让“返回响应”标题固定占第一行，空态内容与真实响应内容都放到第二行，避免隐藏的响应结果面板继续占用 `DockPanel` 的最后填充位。
- 保持现有空态样式不变，只修正布局结构，确保图标与提示文案在响应容器内真正居中。

# 修改文件

- `src/ApixPress.App/Views/Controls/HttpInterfaceWorkbenchView.axaml`
- `src/ApixPress.App/Views/Controls/QuickRequestWorkbenchView.axaml`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 未执行：桌面端手工 smoke test，当前会话未直接打开窗口确认最终视觉位置

# 风险或遗留项

- 本次修正的是响应空态被隐藏响应内容抢占填充位的问题；若你还想继续调整视觉中心点，下一步可以再微调图标与文案之间的间距或整体上移/下移幅度。
