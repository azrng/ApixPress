# 本次目标

- 精简 HTTP 接口设计页顶部区域，只保留“接口说明”输入区。

# 核心改动

- 移除设计页顶部的“当前摘要”卡片。
- 移除右侧“补充信息”区域及其中的接口目录、默认用例名、SSL 选项。
- 将设计页顶部改为单列布局，仅保留标题说明和“接口说明”多行输入框。

# 修改文件

- `src/ApixPress.App/MainWindow.axaml`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 未执行：桌面端手工 smoke test，当前会话未直接打开窗口核对设计页实际视觉效果

# 风险或遗留项

- 本次仅移除设计页顶部辅助信息展示，不影响接口保存与文档预览绑定的说明内容字段。
