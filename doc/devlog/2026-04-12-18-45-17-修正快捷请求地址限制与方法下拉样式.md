# 本次目标

修正快捷请求编辑区中的方法下拉样式问题，并将快捷请求 URL 输入限制为完整地址。

# 核心改动

- 将快捷请求与 HTTP 接口的请求方法下拉样式调整为更稳定的中性色方案，避免出现浅底白字或发灰的可读性问题。
- 更新快捷请求编辑器文案与输入提示，明确要求输入完整的 `http://` 或 `https://` 地址。
- 在快捷请求发送前和保存前增加 URL 校验，若不是完整地址则直接阻止继续操作并提示用户。
- 保持 HTTP 接口编辑器仍支持相对路径与 BaseUrl 拼接，不改现有接口工作流。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `src/ApixPress.App/ViewModels/RequestWorkspaceTabViewModel.cs`

# 校验情况

- 已执行 `dotnet build Apifox.slnx -p:UseAppHost=false`
- 已执行 `dotnet test Apifox.slnx -p:UseAppHost=false --no-build`
- 已执行轻量启动校验：启动 `Apifox.App` 后进程保持运行，再主动结束进程

# 风险或遗留项

- 当前仅对快捷请求添加了完整地址校验，历史数据里如果已有相对路径快捷请求，重新发送或保存时会被拦截，需要改成完整地址后再继续。
- 本次未做人工视觉验收，如你继续发图，我可以再单独微调方法下拉的边框、内边距和箭头颜色。
