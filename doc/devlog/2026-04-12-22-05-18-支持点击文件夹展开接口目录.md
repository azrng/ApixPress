# 本次目标

- 支持在左侧接口目录中直接点击文件夹行展开或收起子节点。

# 核心改动

- 为目录节点增加 `IsClickable` 判断，让有子节点的文件夹本身也能响应点击。
- 调整左侧目录模板按钮命中逻辑，不再只允许可加载请求节点点击。
- 在保存请求加载命令中加入文件夹节点分支：点击纯文件夹时仅切换展开状态，不打开请求编辑页。

# 修改文件

- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/ViewModels/MainWindowViewModel.cs`
- `src/ApixPress.App/ViewModels/WorkspaceItemViewModels.cs`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 未执行：桌面端手工 smoke test，当前会话未直接打开窗口验证点击文件夹展开效果

# 风险或遗留项

- 当前仅对“纯文件夹节点”切换展开状态；可加载的接口节点仍保持点击即打开，不改原有行为。
