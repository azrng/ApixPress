# 优化接口树点击右侧卡顿

## 本次目标
- 降低点击接口树节点后右侧接口详情出现时的卡顿感。

## 核心改动
- `ProjectWorkspaceContentHostView` 在内容模式未变化且已有宿主内容时直接复用现有视图树。
- 避免接口节点切换仍处于 `RequestEditor` 模式时，重复清空并重挂侧栏与请求编辑器视图。

## 修改文件
- `src/ApixPress.App/Views/Controls/ProjectWorkspaceContentHostView.axaml.cs`
- `TASK.md`

## 校验情况
- 已执行 `dotnet test tests\ApixPress.App.Tests\ApixPress.App.Tests.csproj --filter "FullyQualifiedName~ProjectTabViewModelTests"`，通过 32 项。
- 已执行 `dotnet build Apifox.slnx`，通过。

## 风险或遗留项
- 本次优化只处理同内容模式下的重复视图重挂；如果后续仍感到卡顿，需要继续看接口详情数据加载与参数区控件渲染成本。
