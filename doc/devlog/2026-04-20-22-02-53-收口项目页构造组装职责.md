# 本次目标

继续从长期维护角度收紧 `ProjectTabViewModel`，把构造函数中的子 ViewModel 创建、回调注入与事件接线从父类中移出，继续压缩组合根复杂度。

# 核心改动

- 新增 `ProjectTabComposition`，集中承接项目页子 ViewModel 创建、生命周期协调对象创建，以及统一事件接线与初始导航注入。
- `ProjectTabViewModel` 构造函数改为只负责创建项目副本、准备 fallback tab、消费 composition 结果并完成属性赋值。
- 通过 composition 保留现有公开构造签名与行为，不影响主窗口与测试中的 `new ProjectTabViewModel(...)` 调用方式。
- 本轮完成后，`ProjectTabViewModel` 主文件的构造区明显收缩，组合根职责更聚焦在暴露成员与少量壳层代理。

# 修改文件

- `src/ApixPress.App/ViewModels/ProjectTabComposition.cs`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx --no-restore -v minimal`
- 已执行：`dotnet test Apifox.slnx --no-build --no-restore -v minimal`
- 结果：编译通过，57/57 测试通过

# 风险或遗留项

- 当前 `ProjectTabViewModel` 已不再直接承载大段组装逻辑，但 `ProjectTabComposition` 里仍有一组基于回调的对象创建顺序。
- 下一步若继续治理，更适合评估是否将这些回调依赖进一步规整为显式上下文对象或工厂参数对象，降低组装时的闭包耦合。
