# 修复左侧导航居中偏移

## 本次目标
- 修复项目工作区左侧导航菜单错误垂直居中，恢复为顶部展示。

## 核心改动
- 将项目工作区左侧 `NavMenu` 的 `VerticalAlignment` 从 `Center` 调整为 `Top`。
- 保持现有导航宽度、图标和文案样式不变，仅修正布局对齐行为。

## 修改文件
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`

## 校验情况
- 已执行 `dotnet build Apifox.slnx -p:BaseOutputPath=e:\Work\github\ApixPress\.artifacts\build\`
- 已执行 `dotnet test Apifox.slnx -p:BaseOutputPath=e:\Work\github\ApixPress\.artifacts\testbuild\ --no-restore`
- 默认输出目录上的 `dotnet build` 因运行中的 `Apifox.App.exe` 锁定目标文件未完成，已改用临时输出目录完成验证。

## 风险或遗留项
- 未执行人工界面 smoke test，需结合当前运行中的窗口确认左侧导航视觉位置已符合预期。
