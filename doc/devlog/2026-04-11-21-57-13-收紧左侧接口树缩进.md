# 收紧左侧接口树缩进

## 本次目标
- 修正项目详情页左侧接口管理中，接口目录树与左侧边缘之间留白过大的问题。

## 核心改动
- 调整 `ProjectSidebarCatalogContent` 的左侧外边距，将接口树内容整体向左收紧。
- 保持现有树结构、节点层级和加载逻辑不变，只做局部样式修正。

## 修改文件
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`

## 校验情况
- `dotnet test`：通过（10/10）
- `dotnet build -c Release`：通过
- `dotnet build`：未通过，`src/ApixPress.App/obj/Debug/net10.0/Apifox.App.pdb` 被其他进程占用

## 风险或遗留项
- 本次仅收紧了目录树容器缩进；如果你希望树节点再进一步左贴，后续需要继续微调 `TreeViewItem` 模板层级缩进。
- 当前未执行桌面端实际 smoke test，最终视觉效果仍建议结合界面截图再确认一次。
