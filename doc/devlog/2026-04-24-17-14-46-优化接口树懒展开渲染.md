# 本次目标

- 继续优化接口管理的卡顿，重点降低大项目接口树的初次构建与渲染成本。

# 核心改动

- 为 `ExplorerItemViewModel` 增加延迟子节点加载能力，支持节点保留“可展开但尚未物化子节点”的状态。
- 将 `ProjectWorkspaceTreeBuilder` 的接口树改为只预构建首层节点，目录子节点和接口用例节点在首次展开时再生成。
- 调整接口树同步逻辑，在节点已经展开过的情况下仍能正确同步懒加载子节点，避免增量更新后出现已展开节点不刷新的问题。
- 更新测试辅助函数与新增回归用例，覆盖“接口目录懒加载子节点”的行为。

# 修改文件

- `src/ApixPress.App/ViewModels/ExplorerItemViewModel.cs`
- `src/ApixPress.App/ViewModels/ProjectWorkspaceTreeBuilder.cs`
- `src/ApixPress.App/ViewModels/ProjectWorkspaceCatalogViewModel.cs`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.Support.Core.cs`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`
- `TASK.md`

# 校验情况

- 已执行：`dotnet test tests/ApixPress.App.Tests/ApixPress.App.Tests.csproj -p:BaseOutputPath=C:\Work\github\ApixPress\.artifacts\test-output\`
- 结果：75 项测试全部通过。

# 风险或遗留项

- 这轮主要优化了接口树节点物化时机；如果后续仍有明显卡顿，下一步应继续看右侧编辑区的首次渲染和更深层级节点的虚拟化方案。
