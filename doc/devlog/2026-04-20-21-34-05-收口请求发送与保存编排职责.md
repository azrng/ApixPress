# 本次目标

继续从长期维护角度收紧 `ProjectTabViewModel`，把发送请求、保存当前编辑器、从历史转存与保存 HTTP 用例等流程抽成独立工作流 ViewModel，减少父类上的请求编排壳层。

# 核心改动

- 新增 `ProjectRequestWorkflowViewModel`，集中承接请求发送、当前编辑器保存、历史记录转存快捷请求、快捷请求持久化，以及 HTTP 接口/用例保存流程。
- `ProjectTabViewModel` 改为通过 `Workflow` 子 ViewModel 组合请求发送与保存编排职责，父类不再直接持有相关 request/save 方法和取消令牌状态。
- 删除 `ProjectTabViewModel.RequestExecution.cs`、`ProjectTabViewModel.EditorSave.cs` 与 `ProjectTabViewModel.Persistence.cs`，将原先散落的编排逻辑从父类移出。
- 更新主窗口工作区命令与相关测试，统一改为访问 `Workflow.*`。

# 修改文件

- `src/ApixPress.App/ViewModels/ProjectRequestWorkflowViewModel.cs`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `src/ApixPress.App/ViewModels/MainWindowViewModel.WorkspaceCommands.cs`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx --no-restore -v minimal`
- 已执行：`dotnet test Apifox.slnx --no-build --no-restore -v minimal`
- 结果：编译通过，57/57 测试通过

# 风险或遗留项

- 当前 `ProjectTabViewModel` 仍保留顶层 workspace section 切换、环境切换状态与部分 shell 通知壳层；后续仍可继续沿组合对象方向收口。
- 这轮优先收紧了请求工作流编排，后续如果继续治理，建议评估顶层 section/shell 通知是否值得进一步下沉为更明确的协调对象。
