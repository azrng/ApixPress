# 本次目标

继续从长期维护角度收紧 `ProjectTabViewModel`，把仅用于界面绑定的展示摘要、项目设置静态文案与通知汇总从父类中移出，继续减少 partial 与只读代理属性。

# 核心改动

- 新增 `ProjectTabSummaryViewModel`，集中承接项目页标题、环境摘要、请求历史空态、项目设置侧栏文案与统计文本等展示摘要职责。
- 新增 `ProjectWorkspaceTexts.cs`，将 `ProjectSettingsTexts` 与 `ImportTexts` 从 `ProjectTabViewModel` partial 中移出，供项目设置与导入相关 ViewModel 复用。
- `ProjectTabViewModel` 改为通过 `Summary` 子 ViewModel 提供展示绑定，并把 shell 通知汇总内联回主文件。
- 删除 `ProjectTabViewModel.Texts.cs` 与 `ProjectTabViewModel.ShellState.cs`，将相关视图与主窗口读取点统一切到 `Summary.*`。

# 修改文件

- `src/ApixPress.App/ViewModels/ProjectTabSummaryViewModel.cs`
- `src/ApixPress.App/ViewModels/ProjectWorkspaceTexts.cs`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.WorkspaceActions.cs`
- `src/ApixPress.App/ViewModels/MainWindowViewModel.cs`
- `src/ApixPress.App/Views/Controls/MainWindowTitleBarView.axaml`
- `src/ApixPress.App/Views/Controls/ProjectSettingsWorkspaceView.axaml`
- `src/ApixPress.App/Views/Controls/ProjectWorkspaceSidebarView.axaml`
- `src/ApixPress.App/Views/Controls/QuickRequestWorkbenchView.axaml`
- `src/ApixPress.App/Views/Controls/RequestHistoryDetailView.axaml`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx --no-restore -v minimal`
- 已执行：`dotnet test Apifox.slnx --no-build --no-restore -v minimal`
- 结果：编译通过，57/57 测试通过

# 风险或遗留项

- 当前 `ProjectTabViewModel` 已缩减为 `ProjectTabViewModel.cs`、`WorkspaceActions.cs`、`WorkspaceState.cs` 3 个文件，但父类仍承担子 ViewModel 组合与事件接线职责。
- 下一步若继续治理，更合适的方向是评估这些事件接线与初始化编排是否值得下沉为更明确的协调对象，而不是再回到 `partial` 拆分。
