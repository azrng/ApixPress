# 本次目标

修正项目列表首页上半区布局过于松散、创建区空白过大的问题。

# 核心改动

- 将“新建项目 / 项目说明 / 搜索项目 / 操作按钮”改为同一条工具区布局。
- 去掉首页创建区内导致输入框过窄的表单排布方式，改为直接拉伸输入控件。
- 缩小首页主标题层级，弱化首屏大块留白。

# 修改文件

- `src/ApixPress.App/MainWindow.axaml`

# 校验情况

- 已执行：`dotnet build`

# 风险或遗留项

- 本次只调整首页上半区信息架构，项目卡片区未做进一步视觉优化。
