# 本次目标

继续微调请求编辑区的方法下拉样式，重点优化箭头颜色、内容区留白和整体紧凑度。

# 核心改动

- 收紧快捷请求方法下拉的内边距和最小宽度，让控件更贴合输入框高度。
- 为快捷请求和 HTTP 接口的方法下拉补充模板内 `ContentPresenter` 与 `PathIcon` 样式，统一箭头颜色和内容区右侧留白。
- 保持前一轮完整地址限制逻辑不变，本次只继续调整下拉视觉细节。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/Assets/Styles/Global.axaml`

# 校验情况

- 已执行 `dotnet build Apifox.slnx -p:UseAppHost=false`
- 已执行 `dotnet test Apifox.slnx -p:UseAppHost=false --no-build`

# 风险或遗留项

- 构建时存在运行中的 `Apifox.App` 导致 DLL 复制重试警告，但最终构建成功。
- 当前仍未做人工视觉验收，如你继续发图，我可以再细调下拉弹层、选中项背景和箭头位置。
