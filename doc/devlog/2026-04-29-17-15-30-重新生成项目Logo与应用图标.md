# 重新生成项目 Logo 与应用图标

## 本次目标

- 重新设计 ApixPress 项目 Logo，替换标题栏 Logo 与 Windows 应用图标。
- 保留可维护的矢量源文件，便于后续继续调整品牌色与图形细节。

## 核心改动

- 新增 `AppLogo.svg` 作为 Logo 源文件。
- 替换 `AppLogo.png`，用于应用标题栏显示。
- 替换 `AppIcon.ico`，用于 Windows 应用程序图标。
- 新图形采用 API 路由括号、A 字母结构和请求箭头组合，提升小尺寸可辨识度。

## 修改文件

- `src/ApixPress.App/Assets/AppLogo.svg`
- `src/ApixPress.App/Assets/AppLogo.png`
- `src/ApixPress.App/Assets/AppIcon.ico`
- `TASK.md`

## 校验情况

- 已校验 `AppLogo.png` 为 1024x1024、32 位透明 PNG，四角透明。
- 已校验 `AppIcon.ico` 包含 16、24、32、48、64、128、256 多尺寸图标。
- 已执行 `dotnet build .\Apifox.slnx`，通过，0 警告，0 错误。
- 已执行 `dotnet test .\Apifox.slnx --no-build`，通过 143 项测试。

## 风险或遗留项

- 本次未启动桌面应用进行人工视觉确认；资源已通过尺寸、透明度、ICO 结构与构建测试校验。
