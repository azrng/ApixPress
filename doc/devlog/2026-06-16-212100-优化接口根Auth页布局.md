# 优化接口根 Auth 页布局

## 本次目标
- 将接口根 Auth 页从左侧窄卡片改为全宽工作台布局，减少右侧大面积留白。

## 核心改动
- Auth 页复用 `InterfaceRootWorkbenchSurface`，改为顶部说明、中部配置与规则说明、底部保存操作条的结构。
- 移除旧的 `InterfaceRootAuthPanel` 窄卡片样式，新增 Auth 工作台、表单面板、规则说明、状态提示和操作条样式。
- 扩展根目录工作区布局测试，防止 Auth 页退回窄卡片布局。

## 修改文件
- `src/ApixPress.App/Views/Controls/ProjectInterfaceRootWorkspaceView.axaml`
- `src/ApixPress.App/Assets/Styles/WorkspaceEditorStyles.axaml`
- `tests/ApixPress.App.Tests/Views/WorkbenchLayoutTests.cs`
- `TASK.md`

## 校验情况
- 已执行红灯验证：新增布局测试在旧实现下失败，原因是找不到 `InterfaceRootAuthWorkbench`。
- 已执行：`dotnet test tests/ApixPress.App.Tests/ApixPress.App.Tests.csproj --filter "FullyQualifiedName~WorkbenchLayoutTests.InterfaceRootWorkspace_ShouldUseFullWidthWorkbenchTable" --artifacts-path .artifacts\test-auth-layout`，通过 1 项。
- 已执行：`dotnet test tests/ApixPress.App.Tests/ApixPress.App.Tests.csproj --filter "FullyQualifiedName~WorkbenchLayoutTests" --artifacts-path .artifacts\test-workbench-layout-2`，通过 5 项。
- 已执行：`dotnet build src/ApixPress.App/ApixPress.App.csproj --artifacts-path .artifacts\build-auth-layout-2`，0 警告、0 错误。

## 风险或遗留项
- 完整测试套件执行时发现范围外问题：`ResponseSectionViewModelTests.ApplyResult_ShouldKeepLargeJsonBodyUnformatted` 单跑也失败，原因为大响应体异步回填后测试立即断言；`ProjectTabViewModelTests.RequestEditorWorkspaceView_ShouldInstantiateAfterLoadingHttpInterface` 在完整套件并行时出现 Avalonia 线程冲突，单跑通过。
- 本次未修改 Auth 保存、发送、生效优先级、DTO、服务或数据结构。
