# 开发记录

- 本次目标：在保持日期版本为默认行为的前提下，兼容旧 `VERSION` 文件的显式版本配置能力。
- 核心改动：
  - 调整 `Directory.Build.props`，支持从 `VERSION` 读取旧版显式版本，未命中时回退到 `yyyy.M.d`。
  - 调整 `.github/workflows/release.yml`，发布时恢复对 `VERSION` 的兼容读取。
  - 将根目录 `VERSION` 改为 `auto`，让当前仓库默认仍使用日期版本。
- 修改文件：
  - `Directory.Build.props`
  - `.github/workflows/release.yml`
  - `VERSION`
  - `doc/design/设计文档.md`
  - `TASK.md`
- 校验情况：
  - 已执行 `dotnet msbuild src/ApixPress.App/Apifox.App.csproj -getProperty:Version -getProperty:InformationalVersion -getProperty:AssemblyVersion -getProperty:FileVersion`
  - 已执行 `dotnet msbuild src/ApixPress.App/Apifox.App.csproj -p:ApixPressVersionFile=<temp legacy file> -getProperty:Version -getProperty:InformationalVersion -getProperty:AssemblyVersion -getProperty:FileVersion`
  - 已执行 `dotnet publish src/ApixPress.App/Apifox.App.csproj -c Release -r win-x64 --self-contained true -o artifacts/verify/app`
  - 已执行 `dotnet publish src/ApixPress.Updater/ApixPress.Updater.csproj -c Release -r win-x64 --self-contained true -o artifacts/verify/updater`
  - 已确认 `VERSION=auto` 时解析为 `2026.4.21`
  - 已确认临时旧版 `VERSION=1.0.1.0` 时可解析为 `1.0.1.0`
- 风险或遗留项：
  - 若后续再次在 `VERSION` 中填写显式版本号，构建与发布会优先采用该值，不再自动使用当天日期。
