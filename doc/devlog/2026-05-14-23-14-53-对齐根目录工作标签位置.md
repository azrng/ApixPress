# 对齐根目录工作标签位置

## 本次目标

让根目录页顶部工作标签与接口详情页保持一致，避免根目录标签因额外卡片内缩而下沉、偏移。

## 核心改动

- 移除根目录工作区外层 `PanelCard ProjectWorkspaceCanvas` 包裹。
- 将根目录工作区改为与接口详情页一致的顶层紧凑 `Grid` 布局。
- 增加 XAML 结构回归测试，约束根目录工作区顶部 tab 条不再放进带 padding 的卡片容器。

## 修改文件

- `src/ApixPress.App/Views/Controls/ProjectInterfaceRootWorkspaceView.axaml`
- `tests/ApixPress.App.Tests/Views/WorkbenchLayoutTests.cs`
- `TASK.md`

## 校验情况

- `dotnet test tests/ApixPress.App.Tests/ApixPress.App.Tests.csproj --filter "FullyQualifiedName~InterfaceRootWorkspace_ShouldUseFullWidthWorkbenchTable"`：通过，1 个测试通过。
- `dotnet test tests/ApixPress.App.Tests/ApixPress.App.Tests.csproj`：通过，171 个测试通过。
- `dotnet build ApixPress.slnx`：通过，0 个警告，0 个错误。

## 风险或遗留项

- 未启动桌面应用做人工截图确认；本轮已通过 XAML 结构测试和构建验证布局层级。
