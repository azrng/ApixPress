# 本次目标

移除主窗口顶部标题区和项目列表工具栏中的说明文案，精简首页上方信息展示。

# 核心改动

- 删除主窗口左上角应用名下方的状态说明文本展示。
- 删除首页项目列表工具栏中的辅助说明文本展示。
- 保留现有 ViewModel 状态字段和业务流程，避免影响其他命令逻辑。

# 修改文件

- `src/ApixPress.App/MainWindow.axaml`
- `TASK.md`
- `doc/devlog/2026-04-11-10-13-38-移除主窗口顶部说明文案.md`

# 校验情况

- 已执行 `dotnet build Apifox.slnx -p:UseAppHost=false`
- 已执行 `dotnet test Apifox.slnx -p:UseAppHost=false --no-build`
- 构建过程中存在运行中 `Apifox.App` 占用 DLL 的复制重试警告，但最终构建成功

# 风险或遗留项

- 本次仅移除视图展示，未删除 `StatusMessage` 与 `BrowserStatusText` 的内部状态逻辑。
- 若后续确认这些状态不再需要，可再做一次低风险清理以减少 ViewModel 冗余。
