# 继续收口左侧目录高亮规则

## 本次目标
- 根据最新截图，继续把左侧目录树的高亮和层级视觉收口到更接近目标图。

## 核心改动
- 将目录行的“展开态背景高亮”从通用规则改为仅作用于 `接口` 当前主目录。
- 让 `快捷请求` 回到普通目录行风格，不再因为展开而整体高亮。
- 将 `快捷请求` 标题字重从主目录标题降为普通区块标题，进一步拉开与 `接口` 的主次层级。

## 修改文件
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`

## 校验情况
- `dotnet build -c Release`：通过
- `dotnet test`：通过（10/10）

## 风险或遗留项
- 当前主要收的是目录高亮规则，树节点图标、个别行高和具体默认展开层级仍可能需要结合截图继续微调。
