# 本次目标

- 继续压缩项目工作区左侧接口目录中“接口”节点下方的空白区域，并避免空标题节点渲染出整行留白。

# 核心改动

- 收紧接口目录展开内容的顶部 `Padding`、`Margin`、子树缩进和节点按钮高度，让“接口”下方第一层目录更贴近标题行。
- 为 `ExplorerItemViewModel` 增加 `DisplayTitle` 兜底文案，空标题接口会显示为“未命名接口”，避免出现看起来像空白占位的节点。
- 将左侧接口树和快捷请求树的标题绑定切换到 `DisplayTitle`，并补一条单测锁定该回退行为。

# 修改文件

- `src/ApixPress.App/Assets/Styles/WorkspaceEditorStyles.axaml`
- `src/ApixPress.App/Assets/Styles/WorkspaceSidebarStyles.axaml`
- `src/ApixPress.App/ViewModels/ExplorerItemViewModel.cs`
- `src/ApixPress.App/Views/Controls/ProjectWorkspaceSidebarView.axaml`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`
- `TASK.md`

# 校验情况

- 已执行：`dotnet test tests/ApixPress.App.Tests/ApixPress.App.Tests.csproj -c Release --filter "FullyQualifiedName~ProjectTabViewModelTests"`
- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 未执行：桌面端手工 smoke test，当前会话未直接打开窗口核对最终视觉效果

# 风险或遗留项

- 本次主要收紧的是“接口”展开后的顶部空白和空标题占位；若你截图里那一行其实来自更深层目录数据，还需要结合最新界面继续微调具体层级。
