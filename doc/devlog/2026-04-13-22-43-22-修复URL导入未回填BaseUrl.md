# 本次目标
- 修复 Swagger URL 导入成功后仍显示“未解析出 BaseUrl”的问题。

# 核心改动
- 在 `OpenApiJsonParser` 中增加 URL 导入场景的 BaseUrl 兜底解析。
- 当文档缺少 `servers`、`host`、`basePath` 时，自动根据导入地址反推 BaseUrl。
- 对已入库但 `BaseUrl` 为空的旧 URL 文档，在读取文档列表时也补兜底显示，避免必须删掉重导。
- 补充解析器测试和仓储持久化测试。

# 修改文件
- `src/ApixPress.App/Helpers/OpenApiJsonParser.cs`
- `src/ApixPress.App/Services/Implementations/ApiWorkspaceService.cs`
- `tests/ApixPress.App.Tests/Services/OpenApiJsonParserTests.cs`
- `tests/ApixPress.App.Tests/Repositories/WorkspacePersistenceTests.cs`
- `TASK.md`

# 校验情况
- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 已执行：`dotnet test Apifox.slnx -c Release -p:UseAppHost=false`
- 已确认：`http://localhost:5000/swagger/v1/swagger.json` 可访问，且文档本身未声明 `servers`

# 风险或遗留项
- 当前兜底优先按常见 Swagger/OpenAPI URL 规则推断站点根地址；若后续遇到非常规文档发布路径，仍建议结合样例继续补规则。
