# 继续收紧左侧目录树行态

## 本次改动
- 为 `默认模块` 顶部增加树展开箭头，增强目录树层级感
- 将 `数据模型`、`组件库`、`快捷请求` 行统一为更接近树节点的展示结构
- 将 `+ 新建` 按钮改为更接近参考图的小尺寸胶囊按钮
- 进一步收紧默认模块子层级的缩进与留白

## 影响文件
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`

## 验证
- 已执行 `dotnet build`

## 说明
- 本次继续只调整左侧目录树视觉，不改请求保存、目录树数据和交互逻辑
