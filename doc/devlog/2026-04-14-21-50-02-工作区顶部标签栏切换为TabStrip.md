# 2026-04-14 21:50:02 工作区顶部标签栏切换为 TabStrip

## 本次变更

- 将项目工作区顶部两处自绘标签栏从 `ItemsControl + ToggleButton` 切换为 Avalonia `TabStrip`。
- 保留原有的标签关闭按钮、`GET` 方法徽标、新增标签按钮和更多菜单入口。
- 将工作区激活标签后的状态同步补到 `ActiveWorkspaceTab` 变更回调中，避免切换为 `TabStrip` 后状态文案与页面区域不同步。

## 涉及文件

- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `TASK.md`

## 验证

- 已执行 `dotnet build`
- 已执行 `dotnet test`
