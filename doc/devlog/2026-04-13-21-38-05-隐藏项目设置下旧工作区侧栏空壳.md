# 本次目标

- 修复点击项目设置后仍出现旧工作区中栏空白卡片的问题。

# 核心改动

- 在项目设置状态下隐藏旧的工作区侧栏卡片，让项目设置只使用新拆出的独立布局区域。
- 保持项目设置新布局和原有接口管理、请求历史工作区互不干扰。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/MainWindow.axaml`

# 校验情况

- 已执行 `dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 已执行 `dotnet test Apifox.slnx -c Release -p:UseAppHost=false --no-build`

# 风险或遗留项

- 当前仍未执行真实界面 smoke test，建议继续手动验证“接口管理 / 请求历史 / 项目设置”三个一级区域的切换表现。
