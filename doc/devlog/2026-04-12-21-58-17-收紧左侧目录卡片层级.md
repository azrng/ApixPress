# 本次目标

- 调整项目详情左侧“默认模块 / 快捷请求”区域的层级表现，去掉多余背景和边框。

# 核心改动

- 为“接口”和“快捷请求”目录标题增加无边框样式，去掉标题行卡片描边。
- 将目录展开内容区改为透明背景，移除内部额外卡片层。
- 将接口节点和快捷请求节点改为无边框列表项，仅保留悬停高亮。

# 修改文件

- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 未执行：桌面端手工 smoke test，当前会话未直接打开窗口核对视觉效果

# 风险或遗留项

- 本次只收紧了目录内容层，不调整外层分组结构；如果后续还要继续去掉外层容器感，可以再继续收一层 `SectionCard` 样式。
