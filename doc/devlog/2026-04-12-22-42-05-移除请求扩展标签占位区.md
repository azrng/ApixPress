# 移除请求扩展标签占位区

## 本次目标
- 移除请求编辑区域中暂未启用的扩展标签区，减少无效占位内容。

## 核心改动
- 删除快捷请求编辑器中的 `Cookies`、`Auth`、`前置操作`、`后置操作`、`设置` 五个标签及其占位内容。
- 删除 HTTP 接口详情调试页中的同名五个标签及其占位内容，保持两套请求编辑体验一致。

## 修改文件
- `TASK.md`
- `src/ApixPress.App/MainWindow.axaml`

## 校验情况
- 已执行 `dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 已执行 `dotnet test Apifox.slnx -c Release -p:UseAppHost=false --no-build`

## 风险或遗留项
- 相关 ViewModel 字段当前仍保留，但已无对应入口；如果后续确认这些能力不再需要，可以再做一轮逻辑层清理。
