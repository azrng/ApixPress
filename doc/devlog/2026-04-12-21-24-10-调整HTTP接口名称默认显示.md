# 2026-04-12 21:24:10 调整 HTTP 接口名称默认显示

## 本次变更
- 将 HTTP 接口详情页顶部名称输入框的默认展示改为 `未命名接口`。
- 当底层名称只是系统根据方法和路径自动推导出的 `GET /xxx` 形式时，输入框不再直接回显该自动名。

## 核心实现
- 在 `RequestWorkspaceTabViewModel` 中补充自动生成名称的独立计算方法。
- 在 `ProjectTabViewModel` 中将接口名输入框绑定逻辑改为优先展示 `未命名接口`，同时保留保存时的原有命名兜底策略。

## 验证
- 已执行：`dotnet build Apifox.slnx -p:UseAppHost=false`
- 已执行：`dotnet test Apifox.slnx -p:UseAppHost=false --no-build`
- 说明：构建期间存在 `Apifox.App` 进程占用 DLL 的重试警告，但最终构建成功。
