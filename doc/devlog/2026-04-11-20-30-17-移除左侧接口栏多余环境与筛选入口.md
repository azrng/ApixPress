# 移除左侧接口栏多余环境与筛选入口

## 本次目标
- 继续收口项目详情左侧接口管理栏，移除参考图中不存在的多余 UI 元素。

## 核心改动
- 删除左侧接口管理标题右侧的 `main` 环境下拉。
- 删除搜索框后的筛选按钮图标，仅保留搜索框和加号新增按钮。

## 修改文件
- `TASK.md`
- `src/ApixPress.App/MainWindow.axaml`

## 校验情况
- 已执行：`dotnet build src\ApixPress.App\Apifox.App.csproj`

## 风险或遗留项
- 本次仅做局部 UI 删除调整，未继续改动树缩进、图标和文本间距；若还要继续贴近目标图，可再按截图逐项压细节。
