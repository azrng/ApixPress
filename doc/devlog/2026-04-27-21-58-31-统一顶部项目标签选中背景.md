# 统一顶部项目标签选中背景

## 本次目标
- 修正顶部项目标签选中态内外背景不一致的问题，让项目标签视觉层级更干净。

## 核心改动
- 将 `WorkspaceTabButton` 在 hover、checked、checked+hover、pressed 状态下的背景和边框固定为透明。
- 保留外层 `WorkspaceTabCapsule` 负责选中态背景，避免内层 ToggleButton 默认选中态叠出额外灰底。

## 修改文件
- `src/ApixPress.App/Assets/Styles/WorkspaceShellStyles.axaml`
- `TASK.md`

## 校验情况
- `dotnet build .\src\ApixPress.App\Apifox.App.csproj --no-restore -p:OutDir=C:\Work\github\ApixPress\.artifacts\verify-app\`：通过，0 警告 0 错误。

## 风险或遗留项
- 本次为样式微调，未补自动化 UI 测试；建议在运行界面确认首页标签、选中项目标签、未选中项目标签和关闭按钮 hover 的视觉一致性。
