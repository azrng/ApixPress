# 本次目标

- 移除返回响应区域右上角的校验开关与状态文案，并让空态提示居中显示。

# 核心改动

- 移除 HTTP 接口调试页响应头部中的“校验响应 / 开关 / 状态”一整组控件。
- 同步移除快捷请求响应头部右侧环境文案，统一响应区域头部为单标题布局。
- 将快捷请求与 HTTP 调试页的响应空态改为使用填充式容器包裹，使图标与文案在剩余区域内水平和垂直居中。

# 修改文件

- `src/ApixPress.App/MainWindow.axaml`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 未执行：桌面端手工 smoke test，当前会话未直接打开窗口核对响应空态最终位置

# 风险或遗留项

- 本次仅调整响应区头部与空态展示，不影响真实响应内容、Tab 切换和发送逻辑。
