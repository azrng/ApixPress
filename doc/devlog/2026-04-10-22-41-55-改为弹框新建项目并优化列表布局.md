# 本次目标

将“新建项目”从右侧抽屉改为居中弹框，并优化项目列表页的卡片网格布局。

# 核心改动

- 在主窗口内新增居中弹框遮罩，新建项目改为弹框填写项目名称和备注信息。
- 创建成功后自动关闭弹框，并保持项目列表页刷新后的浏览状态。
- 调整项目列表工具条与卡片区布局，收紧留白并将卡片网格优化为更饱满的列表展示。
- 为弹框遮罩、弹框卡片和项目卡片补充样式资源。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/ViewModels/MainWindowViewModel.cs`
- `src/ApixPress.App/ViewModels/ProjectPanelViewModel.cs`
- `src/ApixPress.App/Assets/Styles/Colors.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`

# 校验情况

- 已执行：`dotnet build`
- 已执行：`dotnet test`
- 已执行：启动 smoke test，应用启动 6 秒内保持存活，随后主动结束进程

# 风险或遗留项

- 当前“新建项目”入口已经改为弹框，但旧的 `CreateProjectDrawerView` 文件仍保留在仓库中，暂未移除。
- 若后续需要更贴近参考图，可继续补项目导入入口和卡片封面图标样式。
