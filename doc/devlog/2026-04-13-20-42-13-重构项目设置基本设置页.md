# 重构项目设置基本设置页

## 本次目标
- 将项目设置左侧“概览信息”改为“基本设置”。
- 将右侧概览区域改成更接近目标图的基础信息清单，并移除用户标注删除的行。

## 核心改动
- 左侧项目设置导航文案改为“基本设置”，右侧页标题同步改为“基本设置”。
- 右侧基础页改为清单式布局，只保留 `项目名称`、`项目 ID`、`简介` 三行，移除图标、语言、项目操作、移动项目等内容。
- “编辑”按钮接入现有项目管理抽屉，并在打开前自动选中当前项目。

## 修改文件
- `TASK.md`
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/MainWindow.axaml.cs`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`

## 校验情况
- 已执行 `dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 已执行 `dotnet test Apifox.slnx -c Release -p:UseAppHost=false --no-build`

## 风险或遗留项
- 当前“编辑”统一进入项目管理抽屉，而不是逐字段行内编辑；如果后续需要完全贴近目标图的单行编辑体验，可以再补一层专用编辑弹窗。
