# 修复工作标签菜单重叠

## 本次目标
- 修复项目工作区顶部“更多”菜单打开后覆盖请求编辑区内容的问题。

## 核心改动
- 将工作标签更多菜单从浮层 `Popup` 调整为工具栏内部的展开菜单行。
- 菜单打开时由顶部工具栏自然预留高度，下方请求名称、请求地址行和参数区同步下移。
- 复用原有菜单卡片与菜单项样式，并新增内联菜单对齐样式。

## 修改文件
- `src/ApixPress.App/Views/Controls/ProjectWorkspaceToolbarView.axaml`
- `src/ApixPress.App/Assets/Styles/WorkspaceSidebarStyles.axaml`
- `TASK.md`

## 校验情况
- `git diff --check` 通过。
- `dotnet build .\src\ApixPress.App\Apifox.App.csproj -p:OutputPath=.artifacts\verify-app\` 通过。
- `dotnet test .\tests\ApixPress.App.Tests\ApixPress.App.Tests.csproj -p:OutDir="$PWD\.artifacts\verify-tests\"` 通过，113 个测试全部通过。

## 风险或遗留项
- 本轮未做自动截图比对；通过 XAML 编译和现有测试确认未破坏绑定与命令链路。
