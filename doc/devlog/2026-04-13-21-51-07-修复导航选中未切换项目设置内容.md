# 本次目标

- 修复左侧导航点击“项目设置”后仅高亮但未真正切换内容区的问题。

# 核心改动

- 为 `SelectedWorkspaceNavigationItem` 增加联动逻辑：当导航选中项变化时，自动同步更新 `SelectedWorkspaceSection`。
- 补充测试验证“选中项目设置导航项”会真正切换到项目设置工作区，而不只是更新选中态。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`

# 校验情况

- 已执行 `dotnet test Apifox.slnx -c Release -p:UseAppHost=false`

# 风险或遗留项

- 这轮主要修复导航状态同步，仍建议继续手动验证左侧三个一级导航的切换表现。
