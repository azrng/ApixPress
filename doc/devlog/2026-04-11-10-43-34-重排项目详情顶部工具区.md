# 本次目标

按参考图重排项目详情页中部工作区顶部工具区，让“快捷请求 / 新建...”与环境选择处于同一排，并更接近标签页视觉。

# 核心改动

- 移除项目详情头部中的环境切换区域，避免与工作区顶部重复。
- 在接口管理默认态和快捷请求编辑态顶部新增标签式工具条。
- 新增“新建...”真实交互，点击后会重置当前快捷请求草稿并进入编辑器。
- 将环境选择和环境设置移动到该工具条右侧，与快捷请求入口同层展示。
- 补充项目工作区顶部标签条的样式。

# 修改文件

- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`
- `doc/devlog/2026-04-11-10-43-34-重排项目详情顶部工具区.md`

# 校验情况

- 已执行 `dotnet msbuild src/ApixPress.App/Apifox.App.csproj /t:Compile /p:UseAppHost=false`
- 已执行 `dotnet test tests/ApixPress.App.Tests/ApixPress.App.Tests.csproj --no-build`
- 测试通过：9/9

# 风险或遗留项

- 测试项目单独执行 `Compile` 时会因为运行中的应用占用 `pdb` 被阻塞。
- 本次主要是工作区顶部的布局与样式调整，仍建议在实际窗口里再看一眼视觉是否还要继续贴近参考图。
