# 本次目标
- 修复线上 `versions.json` 为单对象结构时，检查更新解析失败的问题
- 调整发布脚本，让后续 `versions.json` 稳定输出数组结构

# 核心改动
- `ApplicationUpdateService` 改为先读取 JSON 根节点，兼容数组清单与单对象清单
- 新增单元测试覆盖单对象 `versions.json` 的解析路径
- GitHub Actions 生成清单时为 `ConvertTo-Json` 增加 `-AsArray`，避免单条记录被展开为对象

# 修改文件
- `src/ApixPress.App/Services/Implementations/ApplicationUpdateService.cs`
- `tests/ApixPress.App.Tests/Services/ApplicationUpdateServiceTests.cs`
- `.github/workflows/release.yml`
- `TASK.md`
- `doc/devlog/2026-04-17-23-24-07-兼容更新清单单对象格式.md`

# 校验情况
- 已执行：`dotnet test tests/ApixPress.App.Tests/ApixPress.App.Tests.csproj -c Release --filter ApplicationUpdateServiceTests`
- 结果：通过 3 个测试，0 失败
- 已执行：`dotnet build Apifox.slnx -c Release`
- 结果：构建成功，0 警告，0 错误

# 风险或遗留项
- 当前已发布的旧客户端仍需要重新发布新版本后才能获得兼容解析能力
- 若更新包仍放在私有仓库 release 中，匿名下载 zip 仍可能因权限返回 404，需保证更新源对客户端可访问
