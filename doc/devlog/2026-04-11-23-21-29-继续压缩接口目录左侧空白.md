# 继续压缩接口目录左侧空白

## 本次目标
- 继续缩小接口目录中“用户目录”和下方接口项距离左侧过大的空白。

## 核心改动
- 将自定义目录模板首列宽度从 `10` 继续压缩到 `8`。
- 将无子节点占位宽度从 `10` 收紧到 `4`，减少叶子节点前的无意义留白。
- 将递归子节点容器左边距从 `8` 继续压缩到 `4`。

## 修改文件
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`

## 校验情况
- `dotnet build -c Release`：通过
- `dotnet test`：通过（10/10）

## 风险或遗留项
- 当前已经把这一段层级缩进继续压缩；如果视觉上仍偏空，下一步应继续收紧节点内容内部 `ColumnSpacing`，而不是再单纯加大层级负缩进。
