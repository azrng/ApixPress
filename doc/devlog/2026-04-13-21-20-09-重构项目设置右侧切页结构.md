# 本次目标

- 重构项目设置右侧内容区结构，修复“基本设置”点击后仍然空白的问题。

# 核心改动

- 将项目设置右侧从“头部 + 一个 StackPanel + 一个 ScrollViewer”的并列结构，重构为“头部 + 统一内容容器 + 两个独立 ScrollViewer”的切页结构。
- 基本设置和导入数据现在都通过同一层内容容器切换，避免互相影响布局与显示状态。
- 同时保留现有项目编辑入口与导入数据入口，减少这轮改动对原有交互的影响范围。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/MainWindow.axaml`

# 校验情况

- 已执行 `dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 已执行 `dotnet test Apifox.slnx -c Release -p:UseAppHost=false --no-build`

# 风险或遗留项

- 当前仍未执行真实桌面界面 smoke test，建议继续手动点击“项目设置 -> 基本设置 / 导入数据”确认切换表现与目标图一致。
