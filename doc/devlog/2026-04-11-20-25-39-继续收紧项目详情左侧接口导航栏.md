# 继续收紧项目详情左侧接口导航栏

## 本次目标
- 将项目详情左侧接口管理栏从卡片堆叠样式继续收紧，改成更接近参考图的窄栏导航结构。

## 核心改动
- 将项目详情工作区左侧列宽从 `320` 收窄到 `286`，让整体视觉更接近参考图比例。
- 把左栏顶部调整为“标题 + 环境选择”结构，并新增搜索框、筛选按钮、加号按钮组成的工具条。
- 去掉默认模块与快捷请求外层大卡片，改成更扁平、更紧凑的树形导航展示。
- 为左侧导航补充环境下拉、搜索框、工具按钮、幽灵按钮等专用样式，进一步贴近参考图的视觉层级。

## 修改文件
- `TASK.md`
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`

## 校验情况
- 已执行：`dotnet build src\ApixPress.App\Apifox.App.csproj`
- 已执行：启动 smoke，运行 `dotnet .\src\ApixPress.App\bin\Debug\net10.0\Apifox.App.dll` 5 秒后关闭，确认应用可启动。
- 说明：构建过程中存在本地已打开 `Apifox.App.exe` 的文件占用警告，但 DLL 构建成功，未影响本次 smoke 验证。

## 风险或遗留项
- 目前已明显收紧到参考图风格，但还未做逐像素比对；若你继续发差异图，可以继续把搜索栏、树缩进和图标细节再压近。
