# 本次目标

将标题栏右侧按钮组进一步右移到最右侧，同时继续收紧标题栏整体宽度与占位。

# 核心改动

- 将标题栏列布局从固定空白列改回 `*` 弹性列，确保右侧按钮组贴近窗口控制按钮。
- 缩小标题栏列间距、标题栏内边距、标签容器内边距和标签按钮尺寸。
- 缩小右侧工具按钮尺寸，并压缩它们与窗口控制区之间的间距。

# 修改文件

- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`
- `doc/devlog/2026-04-11-10-51-42-继续右移并收紧标题栏.md`

# 校验情况

- 已复读 `MainWindow.axaml` 与 `Global.axaml`，确认标题栏改为 `Auto,Auto,*,Auto,Auto`
- 已执行 `dotnet test tests/ApixPress.App.Tests/ApixPress.App.Tests.csproj --no-build`
- 测试通过：9/9

# 风险或遗留项

- 本次仅调整标题栏布局与样式，未执行完整 `dotnet build`
- 如果还要继续贴近参考图，可以下一步继续压缩应用名与标签条之间的间距
