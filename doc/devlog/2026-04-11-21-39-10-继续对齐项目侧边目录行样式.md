# 继续对齐项目侧边目录行样式

## 本次改动
- 将 `接口 / 数据模型 / 组件库 / 快捷请求` 目录项改为整行宽度显示，不再是居中的胶囊块
- 去掉目录项左侧多余展开箭头，只保留右侧展开箭头，更接近参考项目
- 为 `接口` 增加 `根目录` 右侧提示文本
- 将 `快捷请求` 行内按钮改为更接近参考图的小圆按钮样式

## 影响文件
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`

## 验证
- 已执行 `dotnet build`
- 已执行启动验证，窗口可正常打开

## 说明
- 本次主要继续对齐左侧目录项的视觉形态，未改保存逻辑与目录树数据
