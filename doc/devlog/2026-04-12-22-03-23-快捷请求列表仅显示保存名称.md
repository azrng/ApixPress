# 本次目标

- 调整左侧快捷请求列表默认展示内容，仅显示保存时填写的请求名称。

# 核心改动

- 在工作区导航重建逻辑中，清空快捷请求节点的默认副标题。
- 保留主标题为快捷请求保存名称，不再附带 `请求方法 + URL`。

# 修改文件

- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 未执行：桌面端手工 smoke test，当前会话未直接打开窗口确认列表最终视觉效果

# 风险或遗留项

- 本次仅调整快捷请求树节点默认显示文案，不影响保存数据与加载逻辑。
