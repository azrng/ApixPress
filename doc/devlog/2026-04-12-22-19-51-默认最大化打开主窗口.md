# 本次目标

- 调整应用默认启动行为，使主窗口打开时直接以全屏展示的最大化状态呈现。

# 核心改动

- 在主窗口 XAML 上设置默认 `WindowState="Maximized"`。
- 保持现有自定义标题栏和窗口状态同步逻辑不变，仅调整初始打开状态。

# 修改文件

- `src/ApixPress.App/MainWindow.axaml`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 未执行：桌面端手工 smoke test，当前会话未直接启动应用验证首次打开窗口状态

# 风险或遗留项

- 当前按“最大化窗口”处理“全屏展示”需求；若后续希望进入无边框系统级全屏模式，再单独扩展为 FullScreen 行为。
