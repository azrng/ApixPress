# 修复接口树打开后右侧空白

## 本次目标
- 修复从左侧接口树点击接口后，顶部标签已打开但右侧内容区仍停留在空白 landing 页的问题。

## 核心改动
- 在 `ProjectTabViewModel.NotifyWorkspaceEditorState()` 中补充 `Shell.NotifyWorkspaceStateChanged()`，让复用 landing 标签切换为 HTTP 接口标签时，右侧壳层立即刷新 `ShowRequestEditorWorkspace` / `ShowInterfaceManagementLanding`。
- 补充 `ProjectTabViewModelTests.LoadWorkspaceItem_ShouldShowRequestEditorWorkspaceWhenReusingLandingTab`，覆盖“初始化 landing 页 -> 左侧点击接口 -> 右侧编辑区应显示”的回归场景。

## 修改文件
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`
- `TASK.md`

## 校验情况
- 已执行 `dotnet test tests\\ApixPress.App.Tests\\ApixPress.App.Tests.csproj --filter ProjectTabViewModelTests -p:OutDir=d:\\GitHub\\ApixPress\\.artifacts\\test-output\\ -p:UseAppHost=false`，通过。
- 已执行 `dotnet msbuild src\\ApixPress.App\\Apifox.App.csproj /t:Compile /p:Configuration=Debug /p:UseAppHost=false`，通过。

## 风险或遗留项
- 本次修复聚焦壳层显隐同步，未补 UI 自动化测试。
- 若后续还发现“切标签后右侧状态延迟”类问题，可继续收紧 `Shell` 与 `Workspace` 的状态通知边界。
