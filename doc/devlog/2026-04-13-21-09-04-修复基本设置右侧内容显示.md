# 本次目标

- 修复项目设置中点击“基本设置”后右侧内容未展示的问题。

# 核心改动

- 将项目设置右侧“基本设置”和“导入数据”内容区的显示条件简化为直接使用当前页签选中状态。
- 去掉右侧内容区对外层项目设置状态的重复叠加判断，降低切换时出现空白页的概率。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/MainWindow.axaml`

# 校验情况

- 已执行 `dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 已执行 `dotnet test Apifox.slnx -c Release -p:UseAppHost=false --no-build`

# 风险或遗留项

- 当前仍未执行真实界面点击 smoke test，建议继续手动点击“基本设置 / 导入数据”确认两侧切换效果。
