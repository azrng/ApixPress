# 继续优化接口切换参数区刷新

## 本次目标
- 继续收敛接口列表切换时的参数区刷新成本，减少 Query/Header/Path 区的连续集合通知。

## 核心改动
- 新增 `BatchObservableCollection<T>`，支持批量替换时抑制逐项通知，最后只发一次 `Reset`。
- 将 `RequestConfigTabViewModel` 的参数集合切换为批量集合，并把 `ApplySnapshot`、`PopulateFromEndpoint`、`Reset` 改为批量替换。
- 补充 `RequestConfigTabViewModelTests`，校验接口快照回填时每个参数集合只发一次重置通知。

## 修改文件
- `src/ApixPress.App/Helpers/BatchObservableCollection.cs`
- `src/ApixPress.App/ViewModels/RequestConfigTabViewModel.cs`
- `tests/ApixPress.App.Tests/ViewModels/RequestConfigTabViewModelTests.cs`
- `TASK.md`

## 校验情况
- 已执行：`dotnet test .\\tests\\ApixPress.App.Tests\\ApixPress.App.Tests.csproj`
- 已执行：`dotnet build .\\Apifox.slnx`

## 风险或遗留项
- 当前已压掉参数集合的逐项通知；若后续仍感到切换卡顿，下一步应继续检查大响应正文格式化、文档预览计算属性和 Avalonia 控件层模板重绘。
