# 本次目标
- 更新下一次 GitHub Actions 发布使用的版本号

# 核心改动
- 将根目录 `VERSION` 从 `1.0.0.0` 更新为 `1.0.1.0`
- 下一次 release workflow 会读取该版本并注入主程序、更新器和 `versions.json`

# 修改文件
- `VERSION`
- `TASK.md`
- `doc/devlog/2026-04-17-23-35-28-更新发布版本号.md`

# 校验情况
- 已执行：读取 `VERSION` 并用 `[Version]` 解析
- 结果：`1.0.1.0` 可正常解析
- 已执行：`dotnet build Apifox.slnx -c Release -p:Version=1.0.1.0`
- 结果：构建成功，0 警告，0 错误

# 风险或遗留项
- 需要推送到 `main` 或手动触发 release workflow 后，线上 release 与 `versions.json` 才会更新到 `1.0.1.0`
