# 继续修复项目详情界面缩小留白

## 本次目标

继续处理项目详情页仍按内容尺寸居中显示的问题，恢复工作台在最大化窗口下的全宽展示。

## 核心改动

- 为主窗口项目工作区 `ItemsControl` 宿主增加可识别样式类和拉伸约束。
- 为项目工作区宿主生成的 `ContentPresenter` 增加水平、垂直拉伸样式。
- 为 `ProjectWorkspaceView` 根控件、根 `Border` 和 `LoadingContainer` 内容对齐补齐拉伸约束，覆盖 Ursa 模板内部内容默认居中的风险。
- 扩展工作台布局测试，覆盖主窗口项目工作区宿主与 `LoadingContainer` 内容对齐。

## 修改文件

- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Views/Controls/ProjectWorkspaceView.axaml`
- `tests/ApixPress.App.Tests/Views/WorkbenchLayoutTests.cs`
- `doc/devlog/2026-05-16-17-19-57-继续修复项目详情界面缩小留白.md`

## 校验情况

- 已执行：`dotnet test tests\ApixPress.App.Tests\ApixPress.App.Tests.csproj --filter "FullyQualifiedName~WorkbenchLayoutTests"`，5 个测试通过。
- 已执行：`dotnet build src\ApixPress.App\ApixPress.App.csproj`，构建通过，0 个警告，0 个错误。
- 已执行红灯验证：新增测试在修复前因主窗口工作区宿主缺少 `ProjectWorkspaceHost` 类失败；扩展测试在修复前因 `LoadingContainer` 缺少内容对齐属性失败。

## 风险或遗留项

- 本次只调整项目详情页外层宿主与加载容器布局，不涉及业务逻辑、数据结构或持久化链路。
- 尚未执行真实窗口截图 smoke test；当前使用 XAML 布局契约测试与项目构建作为等价验证。
