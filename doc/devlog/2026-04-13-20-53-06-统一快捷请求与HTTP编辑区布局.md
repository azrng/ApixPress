# 本次目标

- 统一快捷请求与 HTTP 请求右侧编辑区布局，让顶部请求栏、参数区和响应区保持一致。

# 核心改动

- 为快捷请求新增名称输入绑定，默认显示“未命名请求”，并沿用现有请求名称持久化逻辑。
- 将快捷请求顶部工具条改为与 HTTP 请求一致的两行结构：名称输入框、方法选择、地址输入、发送与保存按钮。
- 将快捷请求的 Params、Body、Headers 区域切换为 HTTP 同款表格式布局，并统一响应卡片样式与空间比例。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`

# 校验情况

- 已执行 `dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 已执行 `dotnet test Apifox.slnx -c Release -p:UseAppHost=false --no-build`

# 风险或遗留项

- 本次主要完成结构和样式对齐，尚未做真实窗口截图级 smoke test，仍建议继续点开快捷请求与 HTTP 请求页面做一轮视觉联调。
