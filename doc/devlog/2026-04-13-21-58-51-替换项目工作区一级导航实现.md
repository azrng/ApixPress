# 本次目标

- 解决左侧一级导航点击“项目设置”后仍出现高亮但内容区不切换的问题。

# 核心改动

- 将项目工作区左侧一级导航从 `NavMenu` 替换为自管的 `ItemsControl + ToggleButton` 结构，避免依赖第三方导航组件的内部选中回写行为。
- 为导航项增加 `IsSelected` 状态，并在工作区 section 变化时统一同步导航高亮。
- 保留现有图标、文案和命令行为，只替换导航实现方式。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `src/ApixPress.App/ViewModels/ProjectWorkspaceNavItemViewModel.cs`

# 校验情况

- 已执行 `dotnet test Apifox.slnx -c Release -p:UseAppHost=false`

# 风险或遗留项

- 当前仍建议继续手动验证接口管理、请求历史、项目设置三项一级导航的视觉与切换表现。
