# 导航轨入口居中展示

## 本次目标
- 调整项目详情页左侧导航轨，让接口管理、请求历史、项目设置这组三个入口在区域内居中展示。

## 核心改动
- 调整 `ProjectWorkspaceNavMenu` 样式，为导航菜单补充垂直居中和横向拉伸，使整组导航入口在导航轨容器内居中布局。

## 修改文件
- `TASK.md`
- `src/ApixPress.App/Assets/Styles/Global.axaml`

## 校验情况
- 已执行 `dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 已执行 `dotnet test Apifox.slnx -c Release -p:UseAppHost=false`

## 风险或遗留项
- 本次仅调整导航轨整体对齐方式，未改动单个导航项尺寸；如果后续还要继续压缩按钮高度或标签行距，可在现有样式基础上继续细调。
