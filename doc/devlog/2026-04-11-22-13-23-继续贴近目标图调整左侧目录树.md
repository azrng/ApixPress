# 继续贴近目标图调整左侧目录树

## 本次目标
- 根据最新参考图，继续把项目详情左侧的接口目录调整成更接近真实目录树的层级表达。

## 核心改动
- 重排 `接口` 与 `快捷请求` 根行，去掉当前版本多余的 `根目录` 文案和快捷操作按钮。
- 为侧栏 `TreeViewItem` 增加与视图模型 `IsExpanded` 的同步绑定，让目录、接口、用例层级按数据默认展开状态展示。
- 收紧默认模块子区块与目录树内容缩进，并微调树节点内边距和展开箭头容器宽度。

## 修改文件
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`

## 校验情况
- `dotnet build -c Release`：通过
- `dotnet test`：通过（10/10）

## 风险或遗留项
- 当前已把结构和层级往目标图收紧，但终端内无法直接做桌面界面截图级验证，仍建议结合你下一张截图继续微调图标、展开态和行高。
- `dotnet test` 过程中存在 `Apifox.App.exe` 被运行中进程占用的复制重试警告，但最终测试通过，未阻断本次交付。
