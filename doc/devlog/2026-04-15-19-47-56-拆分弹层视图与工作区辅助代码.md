# 2026-04-15 19:47 拆分弹层视图与工作区辅助代码

## 本次目标
- 拆分 `MainWindowOverlaysView.axaml`，降低单文件弹层聚合复杂度
- 拆分 `WorkspaceItemViewModels.cs`，修复单文件多顶层类型问题
- 抽离 `ProjectTabViewModel` 内的树构建与文档格式化辅助逻辑

## 主要改动
- 将通知中心、环境管理、快捷请求保存、工作区删除确认、Swagger 导入、导入覆盖确认、设置中心、新建项目弹层拆分为独立控件
- 将 `WorkspaceItemViewModels.cs` 拆分为 5 个独立 ViewModel 文件
- 新增 `ProjectWorkspaceTreeBuilder`、`ProjectHttpDocumentFormatter`、`ProjectTabRequestEntryTypes`
- 让 `ProjectTabViewModel` 改为调用独立 helper，减少树构建和 CURL/文档预览字符串拼装的内聚压力

## 验证
- 已执行 `dotnet build Apifox.slnx`
- 已执行 `dotnet test Apifox.slnx`

## 备注
- `MainWindow.axaml` 与 `Global.axaml` 仍然较大，建议下一轮继续按页面区块与样式域拆分
