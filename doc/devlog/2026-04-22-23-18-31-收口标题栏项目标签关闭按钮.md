# 收口标题栏项目标签关闭按钮

- 任务 ID：T068
- 阶段：阶段 1
- 负责人：Codex

## 本次目标

- 让主窗口标题栏项目标签中的关闭按钮收进与标题同一胶囊区域，不再显示在 tab 外侧。

## 核心改动

- 调整主窗口标题栏项目标签模板，为标题与关闭按钮增加统一的外层胶囊容器。
- 将项目标签的选中态与悬停态样式移动到外层胶囊处理，避免关闭按钮游离在标签壳之外。
- 收紧关闭按钮尺寸和对齐方式，让它稳定落在胶囊内部。

## 修改文件

- `src/ApixPress.App/Views/Controls/MainWindowTitleBarView.axaml`
- `src/ApixPress.App/Assets/Styles/WorkspaceShellStyles.axaml`
- `TASK.md`

## 校验情况

- 已执行：`dotnet msbuild src\ApixPress.App\Apifox.App.csproj /t:Compile /p:Configuration=Debug /p:UseAppHost=false`
- 结果：通过
- 未执行：单元测试
- 原因：本次为标题栏 tab 样式微调，当前未补对应 UI 自动化测试。

## 风险或遗留项

- 若后续标题栏项目标签继续叠加更多状态标记，可能还需要进一步微调胶囊内边距与文本截断策略。
