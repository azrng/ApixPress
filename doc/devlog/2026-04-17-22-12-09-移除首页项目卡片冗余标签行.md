# 本次目标

- 移除首页项目卡片中冗余的标签行，让卡片内容更干净，避免视觉噪音。

# 核心改动

- 删除首页项目卡片中的 `HTTP / 快捷请求 / 环境隔离` 标签行。
- 同步收紧项目卡片内部行结构，避免移除标签后留下空白断层。

# 修改文件

- `src/ApixPress.App/Views/Controls/MainWindowHomeView.axaml`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`

# 风险或遗留项

- 当前卡片已经更简洁，但如果后续还想继续贴近参考图，可能还需要继续压缩卡片高度和标题区留白。
