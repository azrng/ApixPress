# 修正标签栏加号按钮居中

## 本次目标
- 修正顶部标签栏中新增标签页的加号按钮未居中的问题。

## 核心改动
- 为 `ProjectWorkspaceInnerTabAddButton` 补充水平和垂直内容居中设置。
- 将加号按钮从纯字符串 `Content` 改为内嵌居中的 `TextBlock`，确保视觉中心稳定。

## 修改文件
- `TASK.md`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `src/ApixPress.App/MainWindow.axaml`

## 校验情况
- 已执行 `dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 已执行 `dotnet test Apifox.slnx -c Release -p:UseAppHost=false --no-build`

## 风险或遗留项
- 本次仅修正新增按钮的内容对齐；如果后续还要统一 `...` 按钮的视觉基线，可在同一区域继续一起细调。
