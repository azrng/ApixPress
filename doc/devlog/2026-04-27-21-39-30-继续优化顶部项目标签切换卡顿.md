# 继续优化顶部项目标签切换卡顿

## 本次目标
- 降低主窗口顶部项目标签切换时的同步视图重建开销，让已打开项目之间来回切换更顺滑。

## 核心改动
- 将项目工作区内容宿主从“切换项目即清空并重建缓存视图”调整为复用已创建视图，仅刷新子视图 `DataContext`。
- 同步处理请求编辑器宿主与接口配置分栏宿主，避免顶部项目切换时继续触发嵌套重控件重建。

## 修改文件
- `src/ApixPress.App/Views/Controls/ProjectWorkspaceContentHostView.axaml.cs`
- `src/ApixPress.App/Views/Controls/RequestEditorWorkspaceView.axaml.cs`
- `src/ApixPress.App/Views/Controls/HttpInterfaceWorkbenchView.axaml.cs`
- `TASK.md`

## 校验情况
- `dotnet build .\src\ApixPress.App\Apifox.App.csproj --no-restore -p:OutDir=C:\Work\github\ApixPress\.artifacts\verify-app\`：通过，0 警告 0 错误。
- `dotnet test .\tests\ApixPress.App.Tests\ApixPress.App.Tests.csproj --no-restore -p:OutDir=C:\Work\github\ApixPress\.artifacts\verify-tests\`：通过，124 个测试全部通过。
- 默认输出目录构建未完成：当前运行中的 `ApixPress (9516)` 锁定了 `bin\Debug\net10.0` 下的程序文件，因此本次使用独立输出目录完成等价编译验证。

## 风险或遗留项
- 本轮未直接关闭当前运行中的应用做人工 UI smoke test；建议在现有窗口或重启后的新窗口中重点验证顶部项目标签连续切换、接口管理/项目设置/请求历史三种内容模式切换。
