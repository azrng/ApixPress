# 本次目标

继续从长期维护角度收紧 `ProjectTabViewModel`，把 workspace section、导航 rail，以及 landing / editor / history 区域显隐状态抽成独立壳层 ViewModel，继续减少父类上的 partial 堆叠。

# 核心改动

- 新增 `ProjectWorkspaceShellViewModel`，集中承接工作区 section、导航项选中、接口管理落地页与请求编辑区显隐判断。
- `ProjectTabViewModel` 改为通过 `Shell` 子 ViewModel 组合工作区壳层职责，父类不再直接持有导航集合、section 状态与 landing/editor 可见性属性。
- 删除 `ProjectTabViewModel.Navigation.cs` 与 `ProjectTabViewModel.State.cs`，把原先散落在父类中的导航与 section 状态逻辑移出。
- 更新工作区相关视图绑定与 `ProjectTabViewModelTests`，统一改为访问 `Shell.*`。

# 修改文件

- `src/ApixPress.App/ViewModels/ProjectWorkspaceShellViewModel.cs`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.ShellState.cs`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.WorkspaceState.cs`
- `src/ApixPress.App/Views/Controls/ProjectWorkspaceNavigationRailView.axaml`
- `src/ApixPress.App/Views/Controls/ProjectWorkspaceSidebarView.axaml`
- `src/ApixPress.App/Views/Controls/ProjectWorkspaceView.axaml`
- `src/ApixPress.App/Views/Controls/ProjectSettingsWorkspaceView.axaml`
- `src/ApixPress.App/Views/Controls/RequestEditorWorkspaceView.axaml`
- `src/ApixPress.App/Views/Controls/RequestHistoryDetailView.axaml`
- `src/ApixPress.App/Views/Controls/WorkspaceLandingView.axaml`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx --no-restore -v minimal`
- 已执行：`dotnet test Apifox.slnx --no-build --no-restore -v minimal`
- 结果：编译通过，57/57 测试通过

# 风险或遗留项

- 当前 `ProjectTabViewModel` 仍保留顶层通知汇总、文案派生和少量工作区协同方法，后续仍可继续沿组合对象方向收口。
- 这轮优先收紧了 workspace 壳层职责；如果继续治理，下一步更适合评估父类剩余的 `Texts` / shell 通知是否要进一步抽成更明确的协调对象，而不是继续增加 partial 文件。
