# 本次目标

- 提升环境管理弹框左侧环境列表的可读性，并将右侧详情区改成更接近表格信息区的布局。

# 核心改动

- 调整左侧环境列表项的默认态、悬停态和选中态，避免 `ListBox` 默认选中前景色把名称内容冲淡到难以辨认。
- 为环境项补充 `DisplayBaseUrl` 占位文案，在未配置前置 URL 时显示“未配置前置 URL”，避免列表项出现空白副标题。
- 重排右侧详情区为“环境名称头区 + 前置 URL 表格区 + 环境变量表格区”，让信息结构更接近参考图中的分区式展示。

# 修改文件

- `src/ApixPress.App/Views/Controls/EnvironmentManagerDialogView.axaml`
- `src/ApixPress.App/Assets/Styles/DialogStyles.axaml`
- `src/ApixPress.App/ViewModels/ProjectEnvironmentItemViewModel.cs`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 未执行：桌面端手工 smoke test，当前会话未直接打开环境弹框确认最终视觉效果

# 风险或遗留项

- 本次主要提升的是可读性和布局结构；如果后续要进一步贴近参考图 2 的信息密度，还可以继续把右侧变量区补成更强的表格感和空态提示。
