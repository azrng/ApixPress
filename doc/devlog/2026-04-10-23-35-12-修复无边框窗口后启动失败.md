# 本次目标

修复无边框窗口与设置中心改造后应用无法启动的问题，恢复本地可正常打开状态。

# 核心改动

- 排查并确认启动失败原因为样式资源引用了不存在的 `Radius.Pill`。
- 将通知红点和通知项中使用的圆角资源改为项目中已定义的资源，避免应用初始化阶段崩溃。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/Assets/Styles/Global.axaml`

# 校验情况

- 已执行 `dotnet build`，通过。
- 已执行 `dotnet test`，9 个测试全部通过。
- 已执行启动验证，应用可以正常启动并保持运行。

# 风险或遗留项

- 本次为启动级修复，未继续调整无边框窗口的视觉细节。
