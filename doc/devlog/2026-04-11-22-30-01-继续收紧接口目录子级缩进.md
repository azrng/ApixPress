# 继续收紧接口目录子级缩进

## 本次目标
- 继续缩小左侧接口目录中“用户目录”和下方接口行距离左侧过大的空白。

## 核心改动
- 收紧自定义目录模板中的展开箭头占位宽度。
- 将递归子节点容器的左边距从较大的默认值继续压缩。
- 保持目录层级、展开逻辑和点击加载逻辑不变，仅调整子级缩进。

## 修改文件
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`

## 校验情况
- `dotnet build -c Release`：通过
- `dotnet test`：通过（10/10）

## 风险或遗留项
- 当前已经把子级缩进继续左收一档；如果你仍觉得偏大，下一步可以继续把首列占位从 `10` 再压到 `8`，并同步收紧节点内 `ColumnSpacing`。
