# 本次目标

- 收紧请求编辑区中“请求方法 + BaseUrl + 路径”这一行的前半段布局，减少方法框过宽和方法框与 BaseUrl 之间的空白。

# 核心改动

- 将请求地址行内部列定义从固定宽度改为 `Auto`，让请求方法下拉框按内容宽度收缩。
- 将请求方法与 BaseUrl 之间的列间距从 `Space.2` 收紧到 `Space.1`。
- 同步压缩请求方法下拉框自身 `Padding`、`MinWidth`，并收紧 BaseUrl 卡片的 `Padding` 与左外边距。

# 修改文件

- `src/ApixPress.App/Views/Controls/RequestEditorWorkspaceView.axaml`
- `src/ApixPress.App/Assets/Styles/WorkspaceEditorStyles.axaml`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 未执行：桌面端手工 smoke test，当前会话未直接打开请求编辑区确认最终视觉效果

# 风险或遗留项

- 本次只收紧了前半段布局；如果后续还想继续贴近参考图，可以再进一步压缩 BaseUrl 卡片宽度或让长 BaseUrl 更早省略。
