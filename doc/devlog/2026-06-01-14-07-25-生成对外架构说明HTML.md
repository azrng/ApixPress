# 生成对外架构说明 HTML

## 本次目标
- 基于当前项目真实代码，生成一份可直接打开的单文件静态 HTML 架构说明。

## 核心改动
- 新增 `doc/ApixPress-2-architecture.html`，覆盖入口、主业务 Pipeline、外部依赖、核心模块、核心数据模型、数据流向、输出与横切能力。
- 明确当前项目未接入 AI Agent 或模型 Runtime，避免把不存在的能力写成已上线模块。

## 修改文件
- `doc/ApixPress-2-architecture.html`
- `TASK.md`
- `doc/devlog/2026-06-01-14-07-25-生成对外架构说明HTML.md`

## 校验情况
- 已读取入口、依赖、服务、仓储、模型、迁移脚本、测试工程与 README，核对架构信息来源。
- 已检查 HTML 结构包含 `doctype`、`lang`、`charset`、`viewport`，并确认没有外链、脚本、Mermaid、SVG 或 Canvas。
- 已使用 Microsoft Edge headless 以 1500px 宽屏生成截图，确认布局无明显文字重叠、箭头遮挡或图例遮挡。

## 风险或遗留项
- 截图为宽屏首屏和主要内容区验证，未逐屏人工核查完整长页的每个细节。
