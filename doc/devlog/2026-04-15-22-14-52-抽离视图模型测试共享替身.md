# 本次目标

继续做长期维护向的即时治理，抽离 `MainWindowViewModelTests` 与 `ProjectTabViewModelTests` 之间重复的测试替身，降低后续测试演进时的重复修改成本。

# 核心改动

- 新增 `ViewModelSharedTestDoubles.cs`，集中承接以下共用 fake：
  - `FakeEnvironmentVariableService`
  - `FakeRequestCaseService`
  - `FakeRequestExecutionService`
  - `FakeRequestHistoryService`
  - `FakeFilePickerService`
- `MainWindowViewModelTests.Support.cs` 改为复用共享替身，仅保留主窗口专用的项目与设置 fake
- `ProjectTabViewModelTests.Support.Services.cs` 改为轻量 partial 壳文件，避免保留重复实现
- `ProjectTabViewModelTests.cs` 与 `ProjectTabViewModelTests.Support.Core.cs` 同步切换到共享替身引用

# 修改文件

- `tests/ApixPress.App.Tests/ViewModels/ViewModelSharedTestDoubles.cs`
- `tests/ApixPress.App.Tests/ViewModels/MainWindowViewModelTests.Support.cs`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.Support.Core.cs`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.Support.Services.cs`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`
- `TASK.md`

# 校验情况

- `dotnet build` 通过
- `dotnet test` 通过，34/34 通过

# 风险或遗留项

- `FakeApiWorkspaceService` 目前仍保留在 `ProjectTabViewModelTests.Support.Core.cs`，因为其导入预览与覆盖逻辑明显比其它替身更复杂；若后续更多测试需要复用，再单独抽为导入专项 support 更合适
- 本轮仍未执行发布构建与应用级 smoke test
