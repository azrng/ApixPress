# 本次目标

继续压缩 Swagger 导入耗时，重点减少“预检后立即导入”场景中的重复文件读取、重复 URL 下载和重复 JSON 解析。

# 核心改动

- 在 `ApiWorkspaceService` 中新增预检产物缓存，预检阶段生成的 `ParsedDocumentGraph` 与预览结果可直接复用于后续导入。
- 文件导入支持复用预检阶段已读取的内容与已解析的图结构，避免同一次导入流程重复读盘和重复解析。
- URL 导入在原有预检后复用下载能力基础上，进一步复用已解析图结构，减少远程导入的重复 CPU 开销。
- 补充文件导入与 URL 导入的复用测试，直接校验读取/下载次数和解析次数均压缩到一次。

# 修改文件

- `src/ApixPress.App/Services/Implementations/ApiWorkspaceService.cs`
- `tests/ApixPress.App.Tests/Repositories/WorkspacePersistenceTests.ApiWorkspace.cs`
- `TASK.md`

# 校验情况

- 已执行：`dotnet test Apifox.slnx --no-restore`
- 结果：50 个测试全部通过

# 风险或遗留项

- 当前缓存仅用于同一项目、同一来源的一次预检后导入流程，不会跨任务长期保留。
- 若后续仍需继续优化，下一步更值得看的点是预检阶段冲突检查是否还需要把整项目接口全量拉到内存再做比对。
