# 本次目标

- 继续修复左侧一级导航点击后高亮与内容切换脱节的问题。

# 核心改动

- 将项目工作区一级导航从会自带切换状态的 `ToggleButton` 改为普通 `Button`。
- 导航高亮现在只由 ViewModel 的 `IsSelected` 状态驱动，不再依赖控件自身的选中行为。
- 补充测试验证直接执行“项目设置”导航命令会切到项目设置内容区。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `src/ApixPress.App/MainWindow.axaml`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`

# 校验情况

- 已执行 `dotnet test Apifox.slnx -c Release -p:UseAppHost=false`

# 风险或遗留项

- 当前仍建议继续手动验证左侧一级导航的真实点击表现，确认界面已不再出现“高亮但空白”的假状态。
