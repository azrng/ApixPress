# 本次目标

- 纠正快捷请求页面上下区域比例方向，让下方返回响应区域明显大于上方请求配置区。

# 核心改动

- 将快捷请求编辑区内容比例从 `4:3` 调整为 `3:5`，改成上小下大的分配方式。
- 继续压低请求配置卡片最小高度，避免上方区域过度占用垂直空间。
- 继续提高返回响应卡片最小高度，让响应内容区更接近参考图的阅读比例。

# 修改文件

- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 未执行：桌面端手工 smoke test，当前会话未直接打开窗口确认比例视觉效果

# 风险或遗留项

- 本次仍是整体比例调整；如果还要继续贴近目标图的编辑器形态，下一步更适合继续收紧参数区内部结构，而不是再单纯拉高响应区。
