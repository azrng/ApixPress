# 本次目标
- 修复当前版本 `1.0` 与更新清单版本 `1.0.0` 被误判为有更新的问题
- 避免已是最新版本时继续启动 `ApixPress.Updater.exe`

# 核心改动
- 更新 `ApplicationUpdateService` 的版本比较逻辑，将缺省的 Build / Revision 按 `0` 参与比较
- 保留界面展示版本为清单中的原始版本号，避免显示被强制改写
- 新增测试覆盖 `1.0` 与 `1.0.0` 语义等价时不触发更新的场景

# 修改文件
- `src/ApixPress.App/Services/Implementations/ApplicationUpdateService.cs`
- `tests/ApixPress.App.Tests/Services/ApplicationUpdateServiceTests.cs`
- `TASK.md`
- `doc/devlog/2026-04-17-23-31-09-修复等价版本误触发更新器.md`

# 校验情况
- 已执行：`dotnet test tests/ApixPress.App.Tests/ApixPress.App.Tests.csproj -c Release --filter ApplicationUpdateServiceTests`
- 结果：通过 4 个测试，0 失败
- 已执行：`dotnet test tests/ApixPress.App.Tests/ApixPress.App.Tests.csproj -c Release --filter MainWindowViewModelTests`
- 结果：通过 6 个测试，0 失败
- 已执行：`dotnet build Apifox.slnx -c Release`
- 结果：构建成功，0 警告，0 错误

# 风险或遗留项
- 当前已发布的旧客户端仍需要重新构建或发布后才能获得该修复
- 若未来引入预发布版本号规则，需要另行扩展 SemVer 语义比较
