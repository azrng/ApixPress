# 延迟挂载请求编辑器内容区

## 本次目标
- 继续压缩接口管理从空态切到请求编辑态的首次切换耗时，避免底部编辑工作台一次性创建多套大视图。

## 核心改动
- 将 `RequestEditorWorkspaceView` 底部内容区改为按 `Editor.CurrentContentMode` 懒挂载。
- 只在当前模式需要时创建并缓存 `QuickRequestWorkbenchView`、`HttpInterfaceWorkbenchView`、`HttpDocumentWorkspaceView`，不再三套视图同时挂载后靠 `IsVisible` 隐藏。
- 保留已创建视图实例，后续同模式切换直接复用，减少重复控件树构建成本。

## 修改文件
- `src/ApixPress.App/Views/Controls/RequestEditorWorkspaceView.axaml`
- `src/ApixPress.App/Views/Controls/RequestEditorWorkspaceView.axaml.cs`
- `TASK.md`

## 校验情况
- 已执行 `dotnet build .\\src\\ApixPress.App\\Apifox.App.csproj -p:OutputPath=\"$PWD\\.artifacts\\verify-app\\\"`
- 已执行 `dotnet test .\\tests\\ApixPress.App.Tests\\ApixPress.App.Tests.csproj -p:OutDir=\"$PWD\\.artifacts\\verify-tests\\\"`
- 已执行 `git diff --check`

## 风险或遗留项
- 本轮主要优化视图挂载策略，若真实环境仍接近 2 秒，下一步需要继续量化请求详情读取与大型参数列表渲染的各自耗时占比。
