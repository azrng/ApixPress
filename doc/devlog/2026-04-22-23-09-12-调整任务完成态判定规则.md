# 调整任务完成态判定规则

- 任务 ID：T067
- 阶段：治理
- 负责人：Codex

## 本次目标

- 调整根规则中 `REVIEW` 与 `DONE` 的判定方式，让代码任务在构建通过且适用单元测试完成后即可收口为 `DONE`。

## 核心改动

- 将 `REVIEW` 收紧为“验证未完成、存在阻塞或仍需额外确认”的状态。
- 将 `DONE` 明确为“本次应做验证已完成”的状态，并为代码任务补充默认门槛：构建通过，若仓库中有适用单元测试则一并完成。
- 同步更新自动提交规则，使其与新的 `DONE` 判定保持一致。

## 修改文件

- `AGENTS.md`
- `TASK.md`

## 校验情况

- 已执行：重新读取 `AGENTS.md`，确认规则文本已写入且中文显示正常。
- 未执行：`build` / `test`
- 原因：本次仅调整规则文档，按现有规则无需强制执行构建或测试。

## 风险或遗留项

- 现有处于 `REVIEW` 的历史任务未自动批量改状态；后续可按新规则逐条收口。
