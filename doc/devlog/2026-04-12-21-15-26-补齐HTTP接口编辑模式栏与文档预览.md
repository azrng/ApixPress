# 2026-04-12 21:15:26 补齐 HTTP 接口编辑模式栏与文档预览

## 本次变更
- 在 HTTP 接口详情页请求栏上方新增 `调试`、`设计`、`文档预览` 三个模式入口，并在右侧增加接口名输入框。
- 将 HTTP 编辑区拆分为工作台模式与文档预览模式，避免原先层级混在一起看不出重点。
- 在文档预览中仅保留请求与响应相关信息，不展示“共享信息”。

## 核心实现
- 在 `RequestWorkspaceTabViewModel` 增加 HTTP 编辑模式状态。
- 在 `ProjectTabViewModel` 增加模式切换命令、接口名代理属性、文档预览辅助属性，并补充环境切换与配置变化时的刷新通知。
- 在 `MainWindow.axaml` 为 HTTP 编辑器增加模式栏、设计说明区和文档预览布局。
- 在 `Global.axaml` 补充模式按钮、接口名输入框、设计卡片和文档预览卡片样式。

## 验证
- 已执行：`dotnet build Apifox.slnx -p:UseAppHost=false`
- 已执行：`dotnet test Apifox.slnx -p:UseAppHost=false --no-build`
- 已执行：`dotnet publish Apifox.slnx -p:UseAppHost=false`
- 未执行：桌面应用启动后的人工 smoke test
