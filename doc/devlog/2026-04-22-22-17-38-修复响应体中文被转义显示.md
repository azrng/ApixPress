# 修复响应体中文被转义显示

## 本次目标
- 修复请求响应区中 JSON 内容格式化后把中文重新显示成 `\uXXXX` 的问题。

## 核心改动
- 调整 `ResponseSectionViewModel` 中 JSON 美化使用的序列化选项，保留缩进同时关闭中文字符的再次转义。
- 为响应区补充中文回归测试，验证 `\u53c2...` 一类 Unicode 转义会正确显示为中文文本。

## 修改文件
- `src/ApixPress.App/ViewModels/ResponseSectionViewModel.cs`
- `tests/ApixPress.App.Tests/ViewModels/ResponseSectionViewModelTests.cs`
- `TASK.md`

## 校验情况
- 已执行 `dotnet test tests\\ApixPress.App.Tests\\ApixPress.App.Tests.csproj --filter ResponseSectionViewModelTests -p:OutDir=d:\\GitHub\\ApixPress\\.artifacts\\test-output\\ -p:UseAppHost=false`，4 个测试全部通过。

## 风险或遗留项
- 本次仅修复客户端 JSON 格式化展示，不改变服务端实际返回内容。
- 若后续还有其他文本预览链路也做了二次 JSON 序列化，需要再按相同思路排查。
