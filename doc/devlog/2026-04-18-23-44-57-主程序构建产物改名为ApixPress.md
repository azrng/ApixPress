# 本次目标

调整桌面端主程序的构建与发布产物名称，确保最终输出统一为 `ApixPress.exe`。

# 核心改动

- 在主程序项目文件中显式设置 `AssemblyName` 与 `Product` 为 `ApixPress`，让构建与发布输出不再沿用旧的 `Apifox.App` 名称。
- 同步更新 README 中的发布说明，避免文档继续指向旧的主程序产物名。
- 保持项目文件路径与 GitHub Actions 工作流不变，仅修改最终程序集与 exe 产物名，降低发布链路改动风险。

# 修改文件

- `src/ApixPress.App/Apifox.App.csproj`
- `README.md`
- `TASK.md`

# 校验情况

- 已执行：`dotnet publish src\\ApixPress.App\\Apifox.App.csproj -c Release -r win-x64 --self-contained true -o .artifacts\\verify-exe-name ...`
- 已执行：启动 `.artifacts\\verify-exe-name\\ApixPress.exe` 轻量 smoke test，进程 8 秒内保持运行后主动结束
- 结果：发布目录已生成 `ApixPress.exe`

# 风险或遗留项

- 当前仅修改了主程序最终程序集与 exe 产物名，项目文件名仍为 `Apifox.App.csproj`；如果后续要继续统一仓库命名，还可以再单独处理工程文件重命名与文档批量清理。
