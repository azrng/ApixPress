# 2026-04-12 22:31:08 收紧顶部环境工具区

## 本次变更
- 收紧项目详情顶部环境选择区宽度，环境下拉改为显示简短名称。
- 将右侧 `环境设置` 文本按钮改成三横杠图标按钮，减少占位。

## 核心实现
- 在 `ProjectEnvironmentItemViewModel` 中增加 `CompactDisplayName` 供顶部紧凑模式使用。
- 在 `MainWindow.axaml` 中把顶部两个环境下拉切换为 `CompactDisplayName`，并把按钮替换为图标按钮。
- 在 `Global.axaml` 中补充顶部图标按钮样式并收紧下拉框最小宽度。

## 验证
- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 已执行：`dotnet test Apifox.slnx -p:UseAppHost=false --no-build`
