# 继续压平左侧目录项行态

## 本次改动
- 将 `接口 / 数据模型 / 组件库 / 快捷请求` 目录项从块状胶囊继续压成整行目录行
- 去掉目录项左侧多余箭头，仅保留右侧展开箭头
- 为 `接口` 行补充 `根目录` 提示文本
- 调整 `快捷请求` 行内操作按钮尺寸、圆角和布局

## 影响文件
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`

## 验证
- 已执行 `dotnet build`
- 已执行启动验证，窗口可正常打开

## 说明
- 本次仍然只收左侧导航视觉，不改数据与保存逻辑
