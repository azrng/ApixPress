# 对齐返回响应标题与状态摘要

- 时间：2026-04-27 10:01:07
- 任务：T098 对齐返回响应标题与状态摘要
- 阶段：阶段 1
- AI：Codex

## 背景

请求编辑区返回响应卡片当前将状态码、耗时和大小摘要单独放在正文区顶部，占用一行垂直空间，且与“返回响应”标题脱节。需要将状态摘要移动到标题行右侧，让信息层次更紧凑。

## 变更

- 调整 HTTP 接口工作区响应区头部布局，将“返回响应”标题改为左侧标题、右侧状态摘要同一行展示。
- 同步调整快捷请求工作区响应区布局，避免两种工作区的响应头表现不一致。
- 删除响应内容区内部单独的状态摘要行，保留正文与 Header Tab 作为响应主体。

## 验证

- `dotnet build .\src\ApixPress.App\Apifox.App.csproj -p:OutputPath=.artifacts\verify-app\`
- `dotnet test .\tests\ApixPress.App.Tests\ApixPress.App.Tests.csproj -p:OutDir="$PWD\.artifacts\verify-tests\"`
- `git diff --check`

## 风险

- 当前仅完成编译和现有单测验证，未做桌面端人工 smoke test；若需确认极窄窗口下标题与摘要是否换行合理，仍建议在真实界面中点开一次响应区查看。
