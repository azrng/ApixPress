# 本次目标

- 修复当前版本应用无法启动的问题，先恢复主程序可正常进入桌面消息循环。

# 核心改动

- 在 `ServiceBootstrapper` 中补充 `services.ConfigureDefaultJson(_ => { });`，注册 `IJsonSerializer` 及其默认选项。
- 保留前一轮对 `DatabaseInitializer` 的显式注册，确保启动链路中的基础依赖都可解析。

# 修改文件

- `src/ApixPress.App/ServiceBootstrapper.cs`

# 校验情况

- 已执行 `dotnet build`，通过。
- 已执行 `dotnet test --no-build`，6/6 通过。
- 已执行 `dotnet run --project src\ApixPress.App\Apifox.App.csproj` 复现验证，进程不再立即抛出依赖注入异常。
- 已执行 `src\ApixPress.App\bin\Debug\net10.0\Apifox.App.exe` 启动验证，进程启动 8 秒后仍存活。

# 风险或遗留项

- 当前仅修复启动阻塞，第二批 Ursa 组件替换暂未继续推进。
- 机器上存在我验证时拉起的 `Apifox.App` 进程残留记录，若你本地正开着应用，请以你当前实例为准。
