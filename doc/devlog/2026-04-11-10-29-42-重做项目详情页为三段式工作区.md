# 本次目标

按参考图重做项目详情页，将项目工作区调整为左侧三项导航、次级侧栏和主画布结构，并让中间默认只保留一个快捷请求入口。

# 核心改动

- 新增项目详情页三段式布局：左侧仅保留接口管理、请求历史、项目设置三个入口。
- 将接口管理默认态改为单个“快捷请求”入口，点击后再进入真实请求编辑器。
- 将请求历史与项目设置改为独立工作区视图，继续复用现有历史、环境和项目说明逻辑。
- 补充项目详情页导航、入口卡片和设置卡片样式。

# 修改文件

- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`
- `doc/devlog/2026-04-11-10-29-42-重做项目详情页为三段式工作区.md`

# 校验情况

- 已执行 `dotnet build Apifox.slnx -p:UseAppHost=false`
- 已执行 `dotnet test Apifox.slnx -p:UseAppHost=false --no-build`

# 风险或遗留项

- 当前已完成结构与交互切换，但未执行应用级 smoke test，仍建议手动确认窗口内实际视觉效果是否完全符合参考图。
- 快捷请求编辑器主体仍复用现有实现，后续若要进一步贴近参考产品，可继续微调顶部工具条、模块树和留白比例。
