# 本次目标

收窄主窗口顶部标签条区域，避免头部中间的圆角容器被整列拉满。

# 核心改动

- 将顶部标题栏网格从 `Auto,*,Auto,Auto` 调整为 `Auto,Auto,*,Auto,Auto`。
- 让标签条容器按内容宽度收缩，并保留右侧操作按钮和窗口控制按钮的独立间隔区域。

# 修改文件

- `src/ApixPress.App/MainWindow.axaml`
- `TASK.md`
- `doc/devlog/2026-04-11-10-35-03-收窄顶部标签条宽度.md`

# 校验情况

- 已复读 `MainWindow.axaml`，确认顶部标签条位于按内容收缩的 `Auto` 列。
- 执行 `dotnet build Apifox.slnx -p:UseAppHost=false` 时被运行中的 `Apifox.App` 占用输出 DLL 阻塞。
- 尝试改用仓库外输出目录做构建校验时，命中了项目现有的重复程序集特性错误，未能完成本次编译验证。

# 风险或遗留项

- 本次改动仅涉及标题栏列宽，不影响业务逻辑。
- 仍建议关闭正在运行的应用后再补一次常规构建，并顺手确认顶部视觉宽度是否符合预期。
