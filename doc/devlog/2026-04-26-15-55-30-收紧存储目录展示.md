# 收紧存储目录展示

## 本次目标
- 调整设置中心存储页目录区域，只展示存储目录，不再展示具体数据库文件路径。

## 核心改动
- 将区域标题从“数据库存储目录”改为“存储目录”。
- 删除“数据库文件”展示行，只保留目录路径和保存状态说明。

## 修改文件
- `src/ApixPress.App/Views/Controls/SettingsDialogView.axaml`
- `TASK.md`

## 校验情况
- 常规 `dotnet build src\ApixPress.App\Apifox.App.csproj` 因当前运行中的 `ApixPress.exe` 锁定 Debug 输出文件失败。
- 已执行 `dotnet build src\ApixPress.App\Apifox.App.csproj -p:OutputPath=.artifacts\verify-app\`，通过。
- 已执行 `dotnet test tests\ApixPress.App.Tests\ApixPress.App.Tests.csproj --no-build`，107 个测试通过。

## 风险或遗留项
- 本次仅调整设置页展示内容，不改变数据库目录保存和启动读取逻辑。
