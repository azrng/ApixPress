# 本次目标

优化首页顶部红框区域的布局，将原本分离的标题信息卡和项目列表工具栏合并为一个整体容器。

# 核心改动

- 将 `MainWindow.axaml` 首页顶部区域从两张独立卡片调整为一张合并卡片。
- 保留“项目首页”说明、统计信息、项目列表标题、搜索框和“新建项目”按钮，但改成统一卡片内的上下分区布局。
- 在 `Global.axaml` 新增首页顶部工具区样式，为合并后的下半区补充分隔线和内边距。
- 同步更新 `TASK.md` 任务状态。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`

# 校验情况

- 已执行 `dotnet build Apifox.slnx -p:UseAppHost=false`
- 已执行 `dotnet test Apifox.slnx -p:UseAppHost=false --no-build`
- 已执行轻量启动校验：启动 `Apifox.App` 后进程保持运行，再主动结束进程

# 风险或遗留项

- 本次主要解决顶部两段白卡割裂的问题，未继续调整下方项目卡片区的栅格密度和卡片内容层次。
- 当前启动校验仅证明应用可以正常拉起，未在真实窗口中做人工视觉验收；若你继续提供截图，我可以再顺着这个区域做细化微调。
