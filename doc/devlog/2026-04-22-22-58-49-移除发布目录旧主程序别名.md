# 移除发布目录旧主程序别名

- 任务 ID：T066
- 阶段：阶段 3
- 负责人：Codex

## 本次变更

- 删除主程序项目发布后额外复制 `Apifox.App.exe` 的兼容别名步骤。
- 同步更新设计文档中的发布产物说明，明确发布目录默认仅保留 `ApixPress.exe`。

## 涉及文件

- `src/ApixPress.App/Apifox.App.csproj`
- `doc/design/设计文档.md`
- `TASK.md`

## 验证

- 已执行：`dotnet publish src\ApixPress.App\Apifox.App.csproj -c Release -r win-x64 --self-contained true -o .artifacts\verify-no-legacy-exe -p:PublishAot=false -p:PublishTrimmed=false -p:DebugType=None -p:DebugSymbols=false -p:IncludeNativeLibrariesForSelfExtract=true -v minimal`
- 已确认：`.artifacts\verify-no-legacy-exe` 目录下仅存在 `ApixPress.exe`
- 已执行：启动 `.artifacts\verify-no-legacy-exe\ApixPress.exe`，进程 8 秒内保持存活，随后主动结束

## 风险

- 本次移除了旧主程序名兼容别名，历史安装包若仍依赖旧更新器按 `Apifox.App.exe` 重启，跨旧版本直升时可能失去该兼容能力。
