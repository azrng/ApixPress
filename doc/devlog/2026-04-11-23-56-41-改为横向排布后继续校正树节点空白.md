# 改为横向排布后继续校正树节点空白

## 本次目标
- 继续定位和缩小接口树节点前方的隐形空白。

## 核心改动
- 将节点外层从带 `*` 列的 `Grid` 改为纯横向 `StackPanel`。
- 让展开箭头 / 占位与节点内容直接并排，避免 `Grid` 测量造成的额外留白。
- 保持节点内部内容布局、点击行为和展开收起逻辑不变。

## 修改文件
- `src/ApixPress.App/MainWindow.axaml`
- `TASK.md`

## 校验情况
- `dotnet build -c Release`：通过
- `dotnet test`：通过（10/10）

## 风险或遗留项
- `dotnet test` 过程中有运行中的 `Apifox.App.exe` 占用 `Debug` 输出文件的复制重试警告，但最终测试通过。
- 如果视觉上仍有明显空白，剩余问题更可能来自图标/徽标自身宽度或行内对齐，而不是层级外框。
