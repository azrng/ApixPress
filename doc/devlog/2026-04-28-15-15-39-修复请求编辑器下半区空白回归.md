# 修复请求编辑器下半区空白回归

## 本次目标
- 修复右侧请求编辑器上方请求行正常显示、下半区 Params/Body/Headers/返回响应区域空白的问题。

## 核心改动
- `ProjectWorkspaceContentHostView` 在重新附着视觉树时恢复 Shell 状态订阅并刷新内容宿主。
- `RequestEditorWorkspaceView` 在重新附着视觉树时恢复 Editor 状态订阅并刷新请求编辑器内容宿主。
- `HttpInterfaceWorkbenchView` 在重新附着视觉树时恢复当前配置页订阅并刷新 Params/Body/Headers 懒挂载内容。
- 为三处订阅增加幂等标记，避免重复订阅导致多次刷新。

## 修改文件
- `src/ApixPress.App/Views/Controls/ProjectWorkspaceContentHostView.axaml.cs`
- `src/ApixPress.App/Views/Controls/RequestEditorWorkspaceView.axaml.cs`
- `src/ApixPress.App/Views/Controls/HttpInterfaceWorkbenchView.axaml.cs`
- `TASK.md`

## 校验情况
- 已执行：`dotnet test Apifox.slnx`
- 结果：通过，134 个测试全部通过。
- 已执行：`dotnet build Apifox.slnx`
- 结果：构建通过，0 警告，0 错误。

## 风险或遗留项
- 当前测试项目未配置 Avalonia Headless UI 测试基座，未执行自动化窗口截图验证；本次以生命周期代码修复、全量测试和编译作为等价验证。
