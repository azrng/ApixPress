# 本次目标
- 移除项目设置中导入页的“已导入数据”展示区块。

# 核心改动
- 删除导入页中“已导入数据 / 刷新列表 / 列表卡片 / 空状态”的整块 UI。
- 同步调整导入页相关状态提示文案，避免继续出现“查看已导入数据”的描述。

# 修改文件
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `TASK.md`

# 校验情况
- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`

# 风险或遗留项
- 当前仅移除了导入页中的展示区块，导入数据的持久化和接口同步逻辑仍然保留，用于支撑左侧接口树与后续编辑流程。
