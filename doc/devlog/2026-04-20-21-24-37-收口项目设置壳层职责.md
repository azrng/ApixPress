# 本次目标

继续从长期维护角度收紧 `ProjectTabViewModel`，把项目设置二级导航、标题派生与 section 切换命令收口到独立子 ViewModel，减少父类对项目设置壳层状态的直接持有。

# 核心改动

- 新增 `ProjectSettingsShellViewModel`，集中承接项目设置概览/导入数据切换、当前标题/副标题、项目简介派生，以及打开项目设置工作区的命令。
- `ProjectTabViewModel` 改为通过 `Settings` 子 ViewModel 组合项目设置壳层职责，父类移除对应的项目设置状态字段与通知逻辑。
- 删除 `ProjectTabViewModel.ProjectSettingsState.cs` 与 `ProjectTabViewModel.ProjectSettingsNotifications.cs`，并把剩余 section 变化通知并回通用 `State` partial。
- 更新项目设置工作区、左侧导航栏和相关测试，统一改为访问 `Settings.*`。

# 修改文件

- `src/ApixPress.App/ViewModels/ProjectSettingsShellViewModel.cs`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.Navigation.cs`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.ShellState.cs`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.State.cs`
- `src/ApixPress.App/Views/Controls/ProjectSettingsWorkspaceView.axaml`
- `src/ApixPress.App/Views/Controls/ProjectWorkspaceNavigationRailView.axaml`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx --no-restore -v minimal`
- 已执行：`dotnet test Apifox.slnx --no-build --no-restore -v minimal`
- 结果：编译通过，57/57 测试通过

# 风险或遗留项

- 当前 `ProjectTabViewModel` 仍保留顶层 workspace section 切换与请求保存/发送编排等父类壳层职责，后续仍可继续沿组合对象方向收口。
- 这轮优先清理了项目设置内层状态和命令，后续如果继续治理，建议优先评估请求发送与保存编排是否也适合下沉为独立协作对象。
