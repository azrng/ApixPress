# 修复重开项目接口树为空

## 本次目标
- 修复 Swagger 已导入且重复导入会提示存在数据，但关闭后重新打开项目时接口管理树显示为空的问题。

## 核心改动
- 项目初始化时恢复加载已保存请求/接口摘要，用于重建左侧接口树。
- 仍保持导入文档列表与项目 endpoint 明细按需加载，避免重新引入打开项目时全量加载导入数据的性能问题。
- 更新 `ProjectTabViewModelTests`，覆盖初始化时恢复已保存接口摘要，同时不加载导入文档和 endpoint 明细。

## 修改文件
- `src/ApixPress.App/ViewModels/ProjectTabLifecycleCoordinator.cs`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`
- `TASK.md`

## 校验情况
- 已执行 `dotnet test tests\ApixPress.App.Tests\ApixPress.App.Tests.csproj --filter "FullyQualifiedName~ProjectTabViewModelTests"`，通过 32 项。
- 已执行 `dotnet test tests\ApixPress.App.Tests\ApixPress.App.Tests.csproj`，通过 96 项。
- 已执行 `dotnet build Apifox.slnx`，通过。

## 风险或遗留项
- 初始化会多加载一次请求/接口摘要；该查询不加载导入文档 endpoint 明细和参数集合，性能风险低于全量导入数据加载。
