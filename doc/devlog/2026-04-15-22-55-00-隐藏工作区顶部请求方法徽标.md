# 隐藏工作区顶部请求方法徽标

## 本次目标

- 去掉项目工作区顶部请求标签中的 `GET`、`POST`、`DELETE` 等方法徽标，只保留标签标题和原有操作入口。

## 核心改动

- 保留 `ProjectWorkspaceToolbarView` 现有内部标签栏结构、关闭按钮、`+` 和更多菜单。
- 移除标签模板中的方法徽标展示，只显示请求标题文本。
- 不改工作区标签切换、关闭和环境操作链路。

## 修改文件

- `src/ApixPress.App/Views/Controls/ProjectWorkspaceToolbarView.axaml`
- `TASK.md`

## 校验情况

- 已执行：`dotnet build Apifox.slnx`

## 风险或遗留项

- 本次仅调整顶部标签展示，未做真实桌面界面人工 smoke test；建议打开项目工作区确认标签文本、关闭按钮和 `+ / ...` 入口显示正常。
