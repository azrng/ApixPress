# 2026-04-12 21:55:18 修正标签菜单撑高顶部区域

## 本次变更
- 修正点击工作区标签栏 `...` 后菜单参与布局，导致顶部区域被整体撑高的问题。
- 将 `+ / ...` 工具位容器改为固定尺寸，让菜单以悬浮方式显示在工具位下方。

## 核心实现
- 在 `MainWindow.axaml` 中把标签工具位容器设为固定宽高，并调整菜单卡片的对齐与偏移方式。
- 保留现有 `关闭全部标签页 / 关闭当前标签页 / 关闭其它标签页` 命令，不改业务行为，只修布局表现。

## 验证
- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 已执行：`dotnet test Apifox.slnx -p:UseAppHost=false --no-build`
- 未完成：`dotnet publish Apifox.slnx -c Release -p:UseAppHost=false`
- 未完成原因：本机进程 `Huorong Internet Security Daemon` 锁定 `obj\\Release\\net10.0\\Apifox.App.dll`
