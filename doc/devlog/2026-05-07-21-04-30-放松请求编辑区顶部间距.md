# 放松请求编辑区顶部间距

## 本次目标
- 小幅放松请求编辑区顶部区域，避免工作标签、模式切换、名称输入和请求地址行贴得过紧。

## 核心改动
- 为请求编辑工作区顶部与编辑工具区之间增加一个基础行距。
- 为快捷请求和 HTTP 接口编辑顶部两行之间增加一个基础间距。
- 轻微增加模式切换按钮、名称输入框、代码按钮和请求地址行内部控件高度。

## 修改文件
- `src/ApixPress.App/Views/Controls/RequestEditorWorkspaceView.axaml`
- `src/ApixPress.App/Assets/Styles/WorkspaceEditorStyles.axaml`
- `TASK.md`

## 校验情况
- 已执行：`dotnet build src\ApixPress.App\Apifox.App.csproj -o artifacts\build-verify\T144-app`
- 未完成普通输出目录构建：当前运行中的 `ApixPress (21524)` 锁定了 `bin\Debug\net10.0\ApixPress.exe` 和 `ApixPress.dll`。

## 风险或遗留项
- 本次为样式微调，未增加自动化 UI 截图验证。
