# 添加 Loading 效果到异步操作

- 任务：T160
- 阶段：阶段 3
- 变更：
  1. `DialogStyles.axaml`：新增 `ProjectSettingsDangerOperationOverlay` 样式
  2. `WorkspaceDetailStyles.axaml`：新增 `RequestHistoryLoadingOverlay` 样式
  3. `RequestHistoryPanelViewModel.cs`：新增 `IsHistoryLoading` 属性，`LoadHistoryAsync` 中用 `try/finally` 包裹加载状态
  4. `ProjectSettingsWorkspaceView.axaml`：在删除确认弹窗后增加 loading overlay，绑定 `Settings.IsProjectDangerOperationBusy` 和 `Settings.ProjectDangerOperationStatus`
  5. `RequestHistoryDetailView.axaml`：在主内容 Grid 内增加 loading overlay，绑定 `HistoryPanel.IsHistoryLoading`，使用 `Grid.RowSpan="2"` 覆盖整个区域
- 风险：无
- 校验：dotnet build 通过，dotnet test 175 项全部通过
