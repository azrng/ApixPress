# 修复环境设置入口无响应

## 本次目标

- 修复项目详情右上角环境菜单按钮点击无反应的问题，确保可打开环境设置并维护前置 URL 与环境变量。

## 核心改动

- 将项目工作区工具栏右上角环境按钮从跨层 XAML 命令绑定调整为明确的点击事件转发，调用主窗口已有 `OpenEnvironmentManagerCommand`。
- 为环境设置命令补充 ViewModel 回归测试，验证打开项目后可进入当前项目的环境设置状态。

## 修改文件

- `src/ApixPress.App/Views/Controls/ProjectWorkspaceToolbarView.axaml`
- `src/ApixPress.App/Views/Controls/ProjectWorkspaceToolbarView.axaml.cs`
- `tests/ApixPress.App.Tests/ViewModels/MainWindowViewModelTests.cs`
- `TASK.md`

## 校验情况

- 已执行：`dotnet test tests\ApixPress.App.Tests\ApixPress.App.Tests.csproj --filter "FullyQualifiedName~MainWindowViewModelTests"`，通过 21 项测试。
- 已执行：`dotnet test ApixPress.slnx`，通过 163 项测试。
- 未执行真实窗口手工点击验证；本次以命令状态流转测试和全量测试作为等价验证。

## 风险或遗留项

- 仍需在实际应用窗口中手工点击右上角环境按钮确认弹窗位置与遮罩表现符合预期。
