# 固定首页项目卡片宽高

- 任务 ID：T063
- 阶段：阶段 1
- 负责人：Codex

## 本次目标

- 让首页项目列表卡片在换行后仍保持统一宽高，确保上下行的卡片栅格更整齐。

## 核心改动

- 为首页项目列表的 `WrapPanel` 补充固定 `ItemHeight`。
- 为项目卡片外层按钮与内层卡片统一设置固定宽高、最小值和最大值，避免内容长短导致布局槽位表现不一致。

## 修改文件

- `src/ApixPress.App/Views/Controls/MainWindowHomeView.axaml`
- `src/ApixPress.App/Assets/Styles/ProjectBrowserStyles.axaml`
- `TASK.md`

## 校验情况

- 已执行：`dotnet msbuild src\ApixPress.App\Apifox.App.csproj /t:Compile /p:Configuration=Debug /p:UseAppHost=false`
- 结果：通过
- 未执行：单元测试
- 原因：本次为首页卡片布局样式微调，当前未补对应 UI / 视图层自动化测试。

## 风险或遗留项

- 卡片宽高现已固定，若后续首页需要承载更长摘要或更多元信息，可能需要重新调整固定尺寸与截断策略。
