# 本次目标

根据用户补充，明确新版主窗口中间区域只保留“快捷请求”工作区，并要求其为正常可用、带环境变量的请求编辑界面。

# 核心改动

- 更新需求文档，明确项目页中间区域不再放入口卡片，只保留快捷请求编辑器。
- 更新设计文档，将项目页中间区域从“快捷入口区”调整为“快捷请求编辑区”。
- 明确快捷请求需要支持当前环境变量参与 URL、Params、Headers 等字段解析。

# 修改文件

- `TASK.md`
- `doc/requirement.md`
- `doc/design/设计文档.md`

# 校验情况

- 本次仅更新设计文档与任务状态，未执行构建与测试。

# 风险或遗留项

- 当前仍处于阶段 0，等待用户确认后再进入实际界面和业务实现。
