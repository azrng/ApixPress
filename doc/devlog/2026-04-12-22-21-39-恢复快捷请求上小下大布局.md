# 本次目标

- 将快捷请求编辑页恢复为更接近参考图的“上方内容自适应、下方响应区占主屏”的布局比例。

# 核心改动

- 将快捷请求主内容区从固定星号分屏改回 `Auto,*`。
- 为上方请求配置卡片设置高度上限，避免参数区再次占用过多垂直空间。
- 保留较大的响应区最小高度，让下方结果查看区域维持主视觉区域。

# 修改文件

- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 未执行：桌面端手工 smoke test，当前会话未直接打开窗口核对与参考图的接近程度

# 风险或遗留项

- 当前通过“自适应 + 最大高度”控制上方区域；如果参数数量很多，顶部仍会在上限内增长，后续可再考虑更强的内部滚动约束。
