# 本次目标

继续减少项目工作区左侧目录树在保存、删除、导入后的刷新抖动，尽量保留未变化节点，避免整批替换带来的额外 UI 成本。

# 核心改动

- 为 `ExplorerItemViewModel` 增加稳定的 `NodeKey`，并补充节点同步方法，支持在刷新时复用原有节点实例。
- 接口树与快捷请求树的刷新改为按 `NodeKey` 同步，而不是直接清空后整体替换集合。
- 树节点子项变化时补充 `ShowTrailingDot` 刷新，保证节点复用后尾部状态也能正确更新。
- 补充回归测试，验证保存快捷请求和保存 HTTP 接口时，现有接口树节点引用仍保持稳定。

# 修改文件

- `src/ApixPress.App/ViewModels/ExplorerItemViewModel.cs`
- `src/ApixPress.App/ViewModels/ProjectWorkspaceTreeBuilder.cs`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.Persistence.cs`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`
- `TASK.md`

# 校验情况

- 已执行：`dotnet test Apifox.slnx --no-restore`
- 结果：55 个测试全部通过

# 风险或遗留项

- 当前树结构仍会先构造目标节点再做同步，已经明显减少 UI 层整批替换，但如果后续还要继续压 CPU 构建成本，可以再进一步把目标树构造也拆成更细粒度的局部更新。
