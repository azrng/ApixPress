# 收紧生成代码按钮并改造弹窗结构

## 本次目标

- 将请求编辑器顶部的生成代码按钮收紧为更小的图标按钮。
- 将生成代码弹窗改成左侧 `Shell`、右侧 `curl / wget / powershell` 页签布局。

## 核心改动

- 收紧名称输入框宽度与代码图标按钮尺寸，让顶部工具栏更接近参考图的轻量布局。
- 将生成代码弹窗改造成左右分栏结构，左侧暂时仅保留 `Shell` 语言入口，右侧保留 `curl`、`wget`、`powershell` 三个代码页签。
- 扩展 `ProjectHttpDocumentFormatter` 与请求编辑器 ViewModel，补齐 `wget` 与 `powershell` 代码生成功能。
- 更新请求代码回归测试，验证三种代码片段都能随当前请求内容生成。

## 修改文件

- `src/ApixPress.App/ViewModels/ProjectHttpDocumentFormatter.cs`
- `src/ApixPress.App/ViewModels/ProjectRequestEditorViewModel.cs`
- `src/ApixPress.App/Views/Controls/RequestCodeDialogView.axaml`
- `src/ApixPress.App/Assets/Styles/DialogStyles.axaml`
- `src/ApixPress.App/Assets/Styles/WorkspaceEditorStyles.axaml`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`
- `TASK.md`

## 校验情况

- 已执行整套测试：`dotnet test .\Apifox.slnx --no-restore`，通过 154 项测试。
- 已回读弹窗结构、按钮尺寸样式与代码生成绑定，确认左侧仅保留 `Shell`，右侧保留 `curl`、`wget`、`powershell` 三个页签。

## 风险或遗留项

- 本次未执行人工界面 smoke test；当前结论基于自动化测试与 XAML/样式回读。
- 左侧语言栏当前仅保留静态 `Shell` 入口，后续若继续扩展更多语言，需要再补对应数据结构与交互切换。
