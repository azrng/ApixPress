# 继续收紧接口树节点内部间距

## 本次目标
- 继续压缩接口目录中“用户目录”与下方接口项的横向空白感。

## 核心改动
- 将自定义目录模板首列与内容列的 `ColumnSpacing` 收紧到 `2`。
- 将节点内容网格内部列距也同步收紧到 `2`。
- 将 `GET` 徽标右侧间距从 `4` 收紧到 `2`，并去掉节点按钮自身多余的左内边距。
- 将节点图标右外边距从 `3` 收紧到 `1`。

## 修改文件
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`

## 校验情况
- `dotnet build -c Release`：通过
- `dotnet test`：通过（10/10）

## 风险或遗留项
- 当前已经在不破坏层级识别的前提下继续压缩节点内部留白；如果还要更紧，需要再结合图标尺寸一起调整，否则会开始影响可读性。
