# 本次目标

- 修复首页布局优化后引入的启动失败问题，确保应用能够正常拉起。

# 核心改动

- 复现并定位到启动期 XAML 资源解析异常：首页样式引用了仓库中未定义的 `Font.Size.TitleSmall` 和 `Font.Size.TitleLarge`。
- 将相关字号资源改回项目中已存在的 `Font.Size.Title`，避免应用在加载 `ProjectBrowserStyles.axaml` 和 `WorkspaceShellStyles.axaml` 时崩溃。
- 重新执行启动与构建验证，确认应用不再在首页初始化阶段报错退出。

# 修改文件

- `src/ApixPress.App/Assets/Styles/ProjectBrowserStyles.axaml`
- `src/ApixPress.App/Assets/Styles/WorkspaceShellStyles.axaml`
- `TASK.md`

# 校验情况

- 已执行：`dotnet run --project src/ApixPress.App/Apifox.App.csproj -c Release`
- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 说明：`dotnet run` 未再抛出启动异常，应用保持运行直到会话超时，随后已主动结束验证进程

# 风险或遗留项

- 当前已修复启动期资源异常，但首页最终视觉效果仍建议结合最新运行截图继续微调。
