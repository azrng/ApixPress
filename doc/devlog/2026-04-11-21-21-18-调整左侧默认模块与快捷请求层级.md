# 调整左侧默认模块与快捷请求层级

## 本次改动
- 将 `数据模型`、`组件库` 从左侧独立一级区块调整为 `默认模块` 下的子层级
- 保持 `快捷请求` 与 `默认模块` 处于同一级
- 为默认模块子层级补充独立缩进与间距样式

## 影响文件
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`

## 验证
- 已执行 `dotnet build`

## 说明
- 本次只调整项目详情左侧导航的视觉层级，不改接口保存和快捷请求保存逻辑
