# 忽略本地 `.appdata` 目录

## 本次目标

- 将 `src/ApixPress.App/.appdata/` 作为本地环境产物加入忽略规则，避免误提交。

## 核心改动

- 确认 `.appdata/NuGet/NuGet.Config` 仅包含本地 NuGet 源配置。
- 在仓库根 `.gitignore` 中增加 `src/ApixPress.App/.appdata/` 精确忽略规则。
- 在 `TASK.md` 中登记并跟踪本次处理。

## 修改文件

- `.gitignore`
- `TASK.md`

## 校验情况

- 读取并确认 `src/ApixPress.App/.appdata/NuGet/NuGet.Config` 内容为普通 NuGet 源配置。
- 检查仓库内无代码或文档引用 `.appdata`。
- 修改后将通过 `git status` 确认未跟踪目录已被忽略。

## 风险或遗留项

- 当前只忽略 `src/ApixPress.App/.appdata/`，未对其他潜在 `.appdata` 目录做全局忽略，避免误伤未来需要入库的目录。
