# 本次目标

- 为主窗口接入 Ursa 主题，并优先替换可直接复用的 Drawer、PopConfirm、SelectionList、TagInput 组件。

# 核心改动

- 在应用入口接入 `Semi.Avalonia` 与 `Irihi.Ursa.Themes.Semi`，让 Ursa 控件具备完整样式资源。
- 将主窗口右侧内嵌的“用例管理 / 环境变量”面板改为 Ursa Drawer，并新增独立 Drawer 视图承载内容。
- 将“清空历史”和删除类操作改为 Ursa `PopConfirm`，同时把 Body 模式切换改为 `SelectionList`，用例标签录入改为 `TagInput`。
- 补充 `DatabaseInitializer` 显式注册，修复应用启动时的依赖注入异常。
- 修复测试项目中 `RequestCaseService` 的构造参数缺失问题，恢复测试编译与执行。

# 修改文件

- `src/ApixPress.App/Apifox.App.csproj`
- `src/ApixPress.App/App.axaml`
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/MainWindow.axaml.cs`
- `src/ApixPress.App/ServiceBootstrapper.cs`
- `src/ApixPress.App/ViewModels/MainWindowViewModel.cs`
- `src/ApixPress.App/ViewModels/UseCasesPanelViewModel.cs`
- `src/ApixPress.App/Views/Controls/UseCasesDrawerView.axaml`
- `src/ApixPress.App/Views/Controls/UseCasesDrawerView.axaml.cs`
- `src/ApixPress.App/Views/Controls/EnvironmentDrawerView.axaml`
- `src/ApixPress.App/Views/Controls/EnvironmentDrawerView.axaml.cs`
- `tests/ApixPress.App.Tests/Repositories/WorkspacePersistenceTests.cs`
- `TASK.md`
- `.gitignore`

# 校验情况

- 已执行 `dotnet build`，通过。
- 已执行 `dotnet test`，6/6 通过。
- 已执行 `dotnet publish src\ApixPress.App\Apifox.App.csproj -c Debug -o .artifacts\publish --no-build`，通过。
- 已执行发布产物启动验证，进程启动后 5 秒仍存活，随后手动结束进程。

# 风险或遗留项

- 本次桌面端 smoke test 仅验证了进程可启动，未进行自动化 UI 点击回归。
- `SemiTheme` 接入后会对部分原生 Avalonia 控件外观产生全局影响，后续若发现视觉不一致，需要再做样式细调。
