# 本次目标

继续优化新建项目弹框和项目列表卡片的视觉表现，提升整体质感。

# 核心改动

- 收紧新建项目弹框尺寸与表单区布局，减少空白区域。
- 调整弹框标题、表单容器和底部操作按钮样式，让结构更清晰。
- 重做项目卡片层级，增加顶部高亮条、头像字母、双标签和更明确的进入操作提示。
- 优化项目备注默认文案，使无备注项目看起来也更完整。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `src/ApixPress.App/ViewModels/ProjectWorkspaceItemViewModel.cs`

# 校验情况

- 已执行：`dotnet build`
- 已执行：`dotnet test`
- 已执行：启动 smoke test，应用启动 6 秒内保持存活，随后主动结束进程

# 风险或遗留项

- 旧的 `CreateProjectDrawerView` 相关文件仍保留在仓库中，但已不再作为主入口使用。
- 若后续继续精修，可以补充项目排序、分组和导入入口。
