# 预热右侧接口编辑视图

## 本次目标
- 继续降低点击接口列表后右侧编辑区出现时的卡顿。

## 核心改动
- `ProjectWorkspaceContentHostView` 在接口管理模式下同时保留空态页和请求编辑器页，切换时不再重新挂载编辑器视图树。
- `RequestEditorContentHostView` 在 DataContext 接入时提前构造 HTTP 调试视图，减少首次点击接口时的 XAML 构造成本。

## 修改文件
- `src/ApixPress.App/Views/Controls/ProjectWorkspaceContentHostView.axaml.cs`
- `src/ApixPress.App/Views/Controls/RequestEditorContentHostView.axaml.cs`
- `TASK.md`

## 校验情况
- 已执行 `dotnet test tests\ApixPress.App.Tests\ApixPress.App.Tests.csproj --filter "FullyQualifiedName~ProjectTabViewModelTests"`，通过 33 项；因当前运行中的 `ApixPress (37644)` 锁定 Debug exe，过程中出现复制重试警告，但测试通过。
- 已执行 `dotnet test tests\ApixPress.App.Tests\ApixPress.App.Tests.csproj -p:UseAppHost=false`，通过 97 项。
- 已执行 `dotnet build src\ApixPress.App\Apifox.App.csproj -o artifacts\verify\prewarm-editor`，通过。

## 风险或遗留项
- 编辑器视图会在进入接口管理后提前构造，可能把一部分成本前移到项目工作区初次显示；如果仍感觉卡顿，需要继续拆分 HTTP 调试视图内的 Params/Body/Response 子区域。
