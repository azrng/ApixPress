# 本次目标

- 修复项目设置中点击“导入数据”后右侧空白的问题，并调整为先展示导入入口页，再由 OpenAPI / Swagger 入口打开导入表单。

# 核心改动

- 将项目设置左侧“导入数据”入口改为切换右侧内容页，不再直接打开导入弹窗。
- 收紧项目设置右侧导入数据页顶部操作区，移除无关按钮，让页面结构更接近目标稿。
- 保持 OpenAPI / Swagger 卡片作为唯一导入入口，点击后继续打开现有导入表单。
- 在切换项目设置页签时主动关闭导入弹窗，避免残留弹层状态影响右侧展示。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`

# 校验情况

- 已执行 `dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 已执行 `dotnet test Apifox.slnx -c Release -p:UseAppHost=false --no-build`

# 风险或遗留项

- 当前未执行真实界面点击 smoke test，建议继续手动验证“项目设置 -> 导入数据 -> OpenAPI / Swagger”这条完整交互链路。
