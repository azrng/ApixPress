# 本次目标

增强项目工作区左侧“接口管理”导航区域的层级感，解决模块、目录和接口项视觉上过于扁平的问题。

# 核心改动

- 为“默认模块”和“快捷请求”两块增加独立分组容器，避免整段内容挤在同一平面。
- 为展开后的目录内容新增内嵌内容区样式，让“接口”目录与其子项形成更清晰的父子层级。
- 调整树形导航的展开占位、子项缩进、目录按钮和叶子节点样式，使接口项更像独立条目而不是纯文本列表。
- 保持现有命令绑定和数据结构不变，仅优化视图结构与样式表现。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`

# 校验情况

- 已执行 `dotnet build Apifox.slnx -p:UseAppHost=false`
- 已执行 `dotnet test Apifox.slnx -p:UseAppHost=false --no-build`

# 风险或遗留项

- 本次未做运行时人工视觉验收，实际观感仍建议结合最新截图继续微调。
- 当前调整主要集中在左侧目录层级，不涉及中间编辑区和顶部工具栏的视觉统一。
