# 本次目标

移除新建项目弹框中的“自动命名”入口，并继续优化项目列表页与项目卡片的视觉布局。

# 核心改动

- 删除新建项目弹框里的“自动命名”按钮，底部仅保留取消和创建项目操作。
- 调整项目列表页顶部工具栏、状态区和项目卡片网格布局，提升信息层级和整体观感。
- 为工具栏分段按钮、搜索框、状态卡片、项目卡片和弹框补充新的样式效果。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`

# 校验情况

- 已执行：`dotnet build`
- 已执行：`dotnet test`
- 已执行：启动 smoke test，应用启动 6 秒内保持存活，随后主动结束进程

# 风险或遗留项

- 当前旧的 `CreateProjectDrawerView` 相关文件仍保留在仓库中，但已不再是主入口。
- 若后续继续美化，可进一步补项目卡片图标变体和分组/排序入口。
