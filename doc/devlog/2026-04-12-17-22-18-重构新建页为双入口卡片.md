# 本次目标

将详情页点击“新建页”后的右侧内容区改成居中的双入口卡片布局，并去掉与当前流程无关的扩展入口形态。

# 核心改动

- 将新建页内容从“左侧大说明卡 + 右侧信息卡”重构为居中双卡片选择区。
- 仅保留“新建 HTTP 接口”和“快捷请求”两个入口，分别沿用原有命令逻辑。
- 补充新建页双卡片所需样式，包括尺寸、悬停态、图标和底部说明文案。
- 保留顶部环境切换与“新建页 / 环境设置”工具区，不改业务命令和保存逻辑。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`

# 校验情况

- 已执行 `dotnet build Apifox.slnx -p:UseAppHost=false`
- 已执行 `dotnet test Apifox.slnx -p:UseAppHost=false --no-build`
- 已执行轻量启动校验：启动 `Apifox.App` 后进程保持运行，再主动结束进程

# 风险或遗留项

- `build` 过程中测试项目存在一次由 `testhost` 占用测试输出 DLL 的复制重试警告，但最终构建成功。
- 本次未做人工视觉验收；如果你再发最新截图，我可以继续细调双卡片尺寸、间距和图标风格。
