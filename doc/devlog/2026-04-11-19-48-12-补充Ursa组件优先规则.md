# 补充 Ursa 组件优先规则

## 本次目标
- 在 AI 协作规则中补充一条明确约束：桌面 UI 能使用 `Irihi.Ursa` / `Ursa.Avalonia` 现成组件完成时，应优先复用组件，而不是重复自定义实现基础交互。

## 核心改动
- 更新根规则文件 `AGENTS.md`，将该要求加入全局核心规则，作为桌面 UI 开发的默认优先级约束。
- 更新 `ui-AGENTS.md`，在组件规则中补充同一要求，强调优先直接使用或轻量封装现成组件。
- 更新 `TASK.md`，同步本次治理任务状态。

## 修改文件
- `AGENTS.md`
- `ui-AGENTS.md`
- `TASK.md`

## 校验情况
- 已执行：回读 `AGENTS.md`、`ui-AGENTS.md`、`TASK.md`，确认规则文本与中文内容正常。
- 未执行：`dotnet build` / `dotnet test`，原因是本次仅涉及协作规则与文档更新，不包含应用代码变更。

## 风险或遗留项
- 当前仅补充规则声明，后续 UI 实现仍需在具体开发任务中按规则落地检查是否优先采用 `Irihi.Ursa` / `Ursa.Avalonia` 组件。
