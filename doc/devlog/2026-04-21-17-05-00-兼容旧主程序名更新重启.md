# 开发记录

- 本次目标：兼容历史 `Apifox.App.exe` 安装包升级到当前 `ApixPress.exe` 时的更新重启链路。
- 核心改动：
  - 在主程序发布后自动额外生成一个 `Apifox.App.exe` 兼容入口，内容与 `ApixPress.exe` 相同。
  - 让旧版本升级后的旧快捷方式、旧进程名重启目标仍然可用，避免因为主程序改名导致升级后无法重启。
- 修改文件：
  - `src/ApixPress.App/Apifox.App.csproj`
  - `doc/design/设计文档.md`
  - `TASK.md`
- 校验情况：
  - 已执行 `dotnet publish src/ApixPress.App/Apifox.App.csproj -c Release -r win-x64 --self-contained true -o artifacts/verify/legacy-alias-app`
  - 已确认发布目录同时包含 `ApixPress.exe` 与 `Apifox.App.exe`
  - 已执行 `Apifox.App.exe` 兼容启动 smoke test，进程 8 秒内保持存活
  - 已执行 `ApixPress.exe` 启动 smoke test，进程 8 秒内保持存活
- 风险或遗留项：
  - 该兼容方案当前只覆盖 Windows `win-x64` 发布链路。
