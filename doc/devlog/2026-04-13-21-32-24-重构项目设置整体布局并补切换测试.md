# 本次目标

- 修复项目设置中点击“基本设置”后右侧仍然空白的问题，并提升该区域结构稳定性。

# 核心改动

- 将项目设置区域从原先分散在中栏和右栏的两块布局，重构为一个 `ColumnSpan=2` 的独立大块，统一承载左侧项目设置菜单和右侧内容页。
- 保留“基本设置 / 导入数据”两种内容，但让它们在同一内容容器中切换，减少跨区域显示状态不同步的问题。
- 补充 ViewModel 测试，验证“基本设置 / 导入数据”切换命令会正确更新项目设置状态。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/MainWindow.axaml`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`

# 校验情况

- 已执行 `dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 已执行 `dotnet test Apifox.slnx -c Release -p:UseAppHost=false`

# 风险或遗留项

- 这轮已经补了状态切换测试，但仍未做真实桌面界面截图级 smoke test，建议继续手动验证项目设置切页效果。
