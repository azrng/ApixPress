# 请求面板增加生成请求代码

## 本次目标

- 在请求编辑面板增加生成请求代码入口。
- 点击后通过弹框展示当前请求对应的 curl 命令。

## 核心改动

- 在快捷请求和 HTTP 接口请求行操作区增加“生成代码”按钮。
- 在请求编辑器内增加请求代码弹框，使用只读文本框展示 curl 命令。
- 复用已有 `ProjectHttpDocumentFormatter.BuildCurlSnippet` 生成 curl 内容，保持与文档预览中的请求代码一致。
- 为请求代码弹框补充 ViewModel 状态、打开/关闭命令和回归测试。

## 修改文件

- `src/ApixPress.App/ViewModels/ProjectRequestEditorViewModel.cs`
- `src/ApixPress.App/Views/Controls/RequestEditorWorkspaceView.axaml`
- `src/ApixPress.App/Assets/Styles/DialogStyles.axaml`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`
- `TASK.md`

## 校验情况

- 首次执行 `dotnet test .\Apifox.slnx` 时发现 Avalonia `TextBox` 滚动条属性写法错误，已改为 `ScrollViewer.*` 附加属性。
- 已再次执行 `dotnet test .\Apifox.slnx`，通过 147 项测试。

## 风险或遗留项

- 本次未启动桌面应用做人工 UI smoke test；XAML 编译和 ViewModel 回归测试已通过。
