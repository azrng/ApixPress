# 本次目标

移除新建页顶部工具区中不需要的说明文本和“新建页”按钮，进一步收紧页面噪音。

# 核心改动

- 删除新建页顶部中间的标题与说明文本区域。
- 删除右上角工具区中的“新建页”按钮，仅保留环境切换和“环境设置”入口。
- 保持新建页中部双入口卡片、环境切换和现有命令逻辑不变。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/MainWindow.axaml`

# 校验情况

- 已执行 `dotnet build Apifox.slnx -p:UseAppHost=false`
- 已执行 `dotnet test Apifox.slnx -p:UseAppHost=false --no-build`

# 风险或遗留项

- 本次仍未做人工视觉验收，是否还需要继续收紧顶部工具栏间距需要结合最新截图判断。
- 构建前存在运行中的 `Apifox.App` 占用输出 DLL，关闭后已重新构建通过。
