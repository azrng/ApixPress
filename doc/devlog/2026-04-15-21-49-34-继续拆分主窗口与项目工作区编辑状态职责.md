# 本次目标

继续拆分大型 ViewModel，将 `MainWindowViewModel` 中的项目标签同步 / shell 状态广播职责，以及 `ProjectTabViewModel` 中的编辑器状态职责进一步解耦。

# 核心改动

- 新增 `MainWindowViewModel.ProjectTabSync.cs`，承接项目标签创建、同步、移除与集合变化处理
- 新增 `MainWindowViewModel.ShellState.cs`，承接 shell 状态广播、通知中心联动和活动标签 facade 属性刷新
- 新增 `ProjectTabViewModel.EditorState.cs`，承接请求编辑器、文档预览、请求名称映射与编辑器绑定属性
- 在 `MainWindowViewModel` 中补充 `NotifyActiveProjectTabBindings`，合并活动标签切换和 shell 回调时重复的 facade 刷新逻辑
- 将 `ProjectTabViewModel.cs` 主文件压缩到更聚焦的构造、集合和基础状态声明

# 修改文件

- `src/ApixPress.App/ViewModels/MainWindowViewModel.State.cs`
- `src/ApixPress.App/ViewModels/MainWindowViewModel.ProjectTabSync.cs`
- `src/ApixPress.App/ViewModels/MainWindowViewModel.ShellState.cs`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.EditorState.cs`
- `TASK.md`

# 校验情况

- `dotnet build` 通过
- `dotnet test` 通过，30/30 通过

# 风险或遗留项

- 本轮未新增 `MainWindowViewModel` 专项单测，当前主要依赖现有回归测试与编译验证
- 若继续治理 `MainWindowViewModel`，后续仍可再按“通知中心 / 设置中心 / 首页 shell 展示”继续细化 partial 边界
