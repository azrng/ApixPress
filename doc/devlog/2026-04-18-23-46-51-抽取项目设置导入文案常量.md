# 本次目标

将项目设置与 Swagger 导入相关页面中的重复说明文案收口为集中变量，减少散落字符串，提升后续维护可读性。

# 核心改动

- 在 `ProjectTabViewModel` 中新增 `ProjectSettingsTexts` 与 `ImportTexts` 两组局部常量，集中承载导入页标题、说明、副标题和常见状态提示。
- 将项目设置页、导入弹窗、导入空态和输入校验中的重复字面量改为复用常量，避免跨 partial 文件重复写同一段文案。
- 保留现有业务行为不变，仅调整文案来源与组织方式。

# 修改文件

- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.ProjectSettingsState.cs`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.Navigation.cs`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.ImportNavigation.cs`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.Import.cs`
- `TASK.md`

# 校验情况

- 已执行：`dotnet test Apifox.slnx --no-restore`
- 结果：55 个测试全部通过

# 风险或遗留项

- 当前只收口了项目设置与 Swagger 导入这一组文案，仓库内其余模块若也要统一管理，可后续继续按领域分组抽取。
