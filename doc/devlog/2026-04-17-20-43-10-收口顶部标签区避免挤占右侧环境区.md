# 本次目标

- 收口项目工作区顶部标签区，让超出的标签内容被裁切隐藏，并确保右侧环境信息区固定展示。

# 核心改动

- 将项目工作区顶部工具条改为“左侧标签区 + 右侧固定区”两段式布局。
- 左侧标签区内部再拆成“标签视口 + 新增/更多按钮”，并为标签视口增加 `ClipToBounds`，避免标签继续向右撑出覆盖环境区。
- 为工作区标签标题增加 `TextTrimming="CharacterEllipsis"` 和最大宽度，减少超长标题对整行宽度的挤压。

# 修改文件

- `src/ApixPress.App/Views/Controls/ProjectWorkspaceToolbarView.axaml`
- `src/ApixPress.App/Assets/Styles/WorkspaceSidebarStyles.axaml`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 未执行：桌面端手工 smoke test，当前会话未直接打开窗口确认标签裁切后的最终视觉效果

# 风险或遗留项

- 本次先按“超出隐藏、右侧固定”收口；如果后续希望超出时还能左右滚动或自动把激活标签滚入可视区，还需要补一轮交互增强。
