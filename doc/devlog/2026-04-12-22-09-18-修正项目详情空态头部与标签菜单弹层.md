# 2026-04-12 22:09:18 修正项目详情空态头部与标签菜单弹层

## 本次变更
- 新打开项目详情时，左上角恢复为仅显示 `+` 和 `...` 的空态头部，不再显示 `新建...` 或默认标签。
- 工作区标签栏中的 `...` 菜单改为 Popup 弹层，避免与下方内容重叠或互相遮挡。

## 核心实现
- 在 `ProjectTabViewModel` 中增加 `VisibleWorkspaceTabs`，标签栏只展示非空态工作标签。
- 恢复默认工作区为 landing 空态，不再在进入项目详情时自动生成 HTTP 接口标签。
- 在 `MainWindow.axaml` 中把 `...` 菜单从内嵌 Border 改为绑定按钮的 Popup。

## 验证
- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 已执行：`dotnet test Apifox.slnx -p:UseAppHost=false --no-build`
- 已执行：`dotnet publish Apifox.slnx -c Release -p:UseAppHost=false`
