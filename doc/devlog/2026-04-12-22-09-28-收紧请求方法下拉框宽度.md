# 本次目标

- 收紧请求编辑区左侧方法下拉框宽度，避免 `GET` 下拉框右侧出现过多空白。

# 核心改动

- 将快捷请求编辑区方法列由固定宽度改为 `Auto`，不再强制占用 110 宽度。
- 收紧 `MethodSelector` 的最小宽度、内边距和模板内容右侧留白，使下拉框更贴近实际内容长度。

# 修改文件

- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 未执行：桌面端手工 smoke test，当前会话未直接打开窗口核对最终视觉效果

# 风险或遗留项

- 本次主要调整快捷请求编辑区的 `MethodSelector`；如果希望 HTTP 接口编辑区的 `HttpMethodSelector` 也完全统一宽度，还可以继续同步一轮。
