# 本次目标

- 压缩左侧接口目录树中“接口”节点与子文件夹之间的空白区域。

# 核心改动

- 将左侧树节点模板外层 `StackPanel` 的垂直间距从 `2` 收紧为 `0`。
- 将节点内容区内部间距从 `2` 收紧为 `1`。
- 去掉子节点容器顶部额外 `4` 像素留白，仅保留层级缩进。

# 修改文件

- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 未执行：桌面端手工 smoke test，当前会话未直接打开窗口核对树节点最终视觉效果

# 风险或遗留项

- 本次仅收紧目录树模板间距，不影响点击展开、节点加载和目录层级结构。
