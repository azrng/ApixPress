# 本次目标

- 在环境管理弹框的前置 URL 输入区补充小提示文案，并确保保存时自动去掉末尾 `/`。

# 核心改动

- 在环境管理弹框的“默认模块 / 前置 URL”输入框下增加提示文案，明确说明末尾不需要带 `/`。
- 在 `EnvironmentVariableService` 中新增 `BaseUrl` 规整逻辑，保存环境时会去掉首尾空白并移除结尾 `/`。
- 补充仓储级测试，验证 `http://localhost:5172/` 会被保存并重新读取为 `http://localhost:5172`。

# 修改文件

- `src/ApixPress.App/Views/Controls/EnvironmentManagerDialogView.axaml`
- `src/ApixPress.App/Services/Implementations/EnvironmentVariableService.cs`
- `tests/ApixPress.App.Tests/Repositories/WorkspacePersistenceTests.cs`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 已执行：`dotnet test tests/ApixPress.App.Tests/ApixPress.App.Tests.csproj -c Release --no-build --filter "FullyQualifiedName~WorkspacePersistenceTests"`
- 说明：首次直接执行 `dotnet test` 时遇到 Avalonia 资源文件被占用，随后在编译成功后使用 `--no-build` 完成测试验证

# 风险或遗留项

- 当前会统一移除结尾 `/`；如果后续某些特殊场景需要保留尾斜杠，需要再按具体规则细分规整逻辑。
