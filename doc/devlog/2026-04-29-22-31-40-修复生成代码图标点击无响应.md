# 修复生成代码图标点击无响应

## 本次目标

- 修复顶部生成代码图标点击后没有弹窗反应的问题。
- 将顶部生成代码图标再收紧一档，贴近参考图尺寸。

## 核心改动

- 在 `ProjectTabComposition` 中补齐请求编辑器到主窗口壳层的通知链，只在 `IsRequestCodeDialogOpen` 变化时触发壳层刷新，确保点击图标后主窗口能正确显示或关闭生成代码弹窗。
- 避免对编辑器全部属性做壳层监听，防止引入过量 shell 刷新回归。
- 继续收紧顶部代码图标按钮尺寸、内边距和字形大小，让视觉更轻。
- 更新回归测试，验证打开/关闭生成代码弹窗时会触发对应的壳层状态通知。

## 修改文件

- `src/ApixPress.App/ViewModels/ProjectTabComposition.cs`
- `src/ApixPress.App/Assets/Styles/WorkspaceEditorStyles.axaml`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`
- `TASK.md`

## 校验情况

- 已执行整套测试：`dotnet test .\Apifox.slnx --no-restore`，通过 154 项测试。
- 校验过程中曾因运行中的 `ApixPress.exe` 锁定输出文件导致测试失败，关闭进程后已重新通过。

## 风险或遗留项

- 本次未执行人工界面 smoke test；当前结论基于自动化测试与代码回读。
- 当前图标仍采用 `</>` 文本形式，若后续还需要更接近设计稿，可再替换成专用矢量图标资源。
