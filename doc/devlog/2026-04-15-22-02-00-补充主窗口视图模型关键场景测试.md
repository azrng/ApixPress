# 本次目标

继续推进 `T010` 的验证收口，为 `MainWindowViewModel` 补充关键场景测试，降低后续继续拆分 partial 时的回归风险。

# 核心改动

- 新增 `MainWindowViewModelTests.cs`，覆盖以下 4 个关键场景：
  - 初始化时加载项目列表与 shell 设置
  - 重复打开同一项目时复用已有标签
  - 当前项目被删除后刷新工作区，自动回收标签并回到首页状态
  - 打开通知中心时自动标记消息为已读并同步 shell 状态
- 新增 `MainWindowViewModelTests.Support.cs`，为主窗口视图模型提供项目、设置、环境、请求、历史、导入等测试替身服务
- 将 `TASK.md` 中 `T010` 的本轮说明同步更新为测试补强内容

# 修改文件

- `tests/ApixPress.App.Tests/ViewModels/MainWindowViewModelTests.cs`
- `tests/ApixPress.App.Tests/ViewModels/MainWindowViewModelTests.Support.cs`
- `TASK.md`

# 校验情况

- `dotnet build` 通过
- `dotnet test tests/ApixPress.App.Tests/ApixPress.App.Tests.csproj --filter MainWindowViewModelTests` 通过，4/4 通过
- `dotnet test` 通过，34/34 通过

# 风险或遗留项

- 当前 `MainWindowViewModel` 的测试替身仍是按需最小实现，若后续继续覆盖设置自动保存、环境管理弹层等路径，可再拆成共享 support
- 本轮仍未执行发布构建与应用级 smoke test
