# 优化接口点击详情打开速度

## 本次目标
- 降低点击接口树节点后右侧详情打开的等待感。

## 核心改动
- `ProjectWorkspaceCatalogViewModel.LoadWorkspaceItem` 改为先用树节点已有摘要打开右侧工作标签。
- 完整请求详情继续异步加载，加载完成后再补齐参数、Header、Body 等内容。
- 增加回归测试，模拟详情加载被阻塞时右侧编辑器仍能先进入 `RequestEditor` 模式，随后补齐参数。

## 修改文件
- `src/ApixPress.App/ViewModels/ProjectWorkspaceCatalogViewModel.cs`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`
- `tests/ApixPress.App.Tests/ViewModels/ViewModelSharedTestDoubles.cs`
- `TASK.md`

## 校验情况
- 已执行 `dotnet test tests\ApixPress.App.Tests\ApixPress.App.Tests.csproj --filter "FullyQualifiedName~ProjectTabViewModelTests"`，通过 33 项。
- 已执行 `dotnet test tests\ApixPress.App.Tests\ApixPress.App.Tests.csproj`，通过 97 项。
- 已执行 `dotnet build Apifox.slnx`，通过。

## 风险或遗留项
- 右侧会先展示摘要数据，再补全参数详情；这是为了优先保证点击响应速度。
