# 本次目标

继续收紧“新建页”落地页的双入口卡片样式，让布局更贴近参考图的视觉比例与信息密度。

# 核心改动

- 去掉双入口卡片下方的补充说明文案，减少中部区域噪音。
- 将两张入口卡片调整为更紧凑的尺寸与更明确的上下重心，标题位置更靠近卡片底部。
- 补充入口容器样式，优化双卡片之间的间距和整体最大宽度。
- 保持原有 `新建 HTTP 接口` 与 `快捷请求` 两个命令入口不变。

# 修改文件

- `TASK.md`
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`

# 校验情况

- 已执行 `dotnet build Apifox.slnx -p:UseAppHost=false`
- 已执行 `dotnet test Apifox.slnx -p:UseAppHost=false --no-build`

# 风险或遗留项

- 本次仍未做人工视觉验收，是否与目标图完全一致还需要你继续看图确认。
- 当前只继续打磨新建页 landing 区，不涉及进入编辑器后的表单与工具栏布局。
