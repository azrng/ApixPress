# 本次目标

- 修正请求 Body 等多行输入框仍然垂直居中的问题，使其从左上角开始输入。

# 核心改动

- 为 `AcceptsReturn=True` 的多行 `TextBox` 显式设置 `HorizontalAlignment=Stretch`、`VerticalAlignment=Stretch`。
- 为 `RawEditorCard` 内的 `TextBox` 单独补充拉伸样式，确保原始 Body 编辑器铺满容器并从顶部开始显示占位文本和输入内容。

# 修改文件

- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`

# 校验情况

- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 未执行：桌面端手工 smoke test，当前会话未直接打开窗口确认多行输入框视觉效果

# 风险或遗留项

- 本次主要覆盖多行输入框；若后续仍有个别特定控件模板不生效，需要继续针对该控件模板部件补样式。
