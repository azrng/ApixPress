# 微调标题栏与工作区间隔

## 本次目标

- 在主窗口顶部项目标签区与下方工作区之间补回一点点纵向间隔。
- 保持整体紧凑，但避免两层内容完全贴死。

## 核心改动

- 仅调整 `ProjectWorkspaceView` 外层工作区容器的上边距。
- 将工作区外层 `Margin` 从 `12,0,12,12` 微调为 `12,4,12,12`，让标题栏下方恢复轻微呼吸感。

## 修改文件

- `src/ApixPress.App/Views/Controls/ProjectWorkspaceView.axaml`
- `TASK.md`

## 校验情况

- 已执行：`dotnet test .\Apifox.slnx --no-restore`
- 结果：154 项测试全部通过。
- 已执行应用启动等价验证：直接运行 `src/ApixPress.App/bin/Debug/net10.0/ApixPress.dll`
- 结果：应用可持续运行 8 秒，无启动期异常，随后主动结束验证进程。
- 补充说明：首次执行测试时遇到运行中的 `ApixPress` 进程锁住 `ApixPress.exe`，清理进程后已重新通过，不属于本次样式改动问题。

## 风险或遗留项

- 本次只补了标题栏与工作区之间的轻微外层间隔，没有继续改内部工具栏高度。
- 如果你觉得还要再松一点或再紧一点，下一轮只需要继续微调这个上边距即可。
