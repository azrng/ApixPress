# 继续压缩接口树默认缩进

## 本次目标
- 继续修正项目详情页左侧接口目录前方空白过大的问题，让树节点更贴近参考图。

## 核心改动
- 针对 `ProjectSidebarTree` 局部覆写 `TreeViewItem` 模板部件样式。
- 收紧展开箭头容器宽度，并去掉标题承载区域的额外左边距。
- 保持接口树数据结构、展开逻辑和点击加载逻辑不变。

## 修改文件
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`

## 校验情况
- `dotnet build -c Release`：通过
- `dotnet test`：通过（10/10）

## 风险或遗留项
- 本次调整依赖 Avalonia 当前 `TreeViewItem` 模板部件名；后续若升级主题版本，需要回归检查这一局部样式是否仍然生效。
- 当前未做桌面端截图级 smoke test，建议结合你下一轮截图继续确认最终视觉是否已达到预期。
