# 调整项目详情接口树与 HTTP 请求页

## 本次目标
- 将项目详情左侧接口目录调整为“默认模块 → 接口 → 接口目录分组 → 具体接口 → 接口用例”的真实层级。
- 将 HTTP 请求页改为更接近参考图的单页结构：顶部请求行、中部配置标签、底部返回响应。

## 核心改动
- 调整 `ProjectTabViewModel` 的左侧树构建逻辑，按接口目录、接口节点、接口用例节点重新组织保存数据，并补充目录/用例数量展示。
- 扩展 `ExplorerItemViewModel`，增加方法徽标、节点图标和树节点显示状态，让左侧目录更贴近参考图的层级表现。
- 重构 `MainWindow.axaml` 中的 HTTP 编辑区布局，移除原来的右侧设置栏结构，改为顶部 URL 行、标签配置区、底部响应区。
- 为 HTTP 标签页补齐 `Cookies`、`Auth`、`前置操作`、`后置操作`、`设置` 页签，并把接口目录、接口名称、接口用例和保存用例入口放入设置页。
- 更新全局样式资源，补充树节点、HTTP 请求行、参数表头、响应区等新样式。

## 修改文件
- `TASK.md`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `src/ApixPress.App/ViewModels/WorkspaceItemViewModels.cs`
- `src/ApixPress.App/ViewModels/RequestWorkspaceTabViewModel.cs`
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`

## 校验情况
- 已执行：`dotnet build src\ApixPress.App\Apifox.App.csproj`
- 已执行：启动 smoke，运行 `dotnet .\src\ApixPress.App\bin\Debug\net10.0\Apifox.App.dll` 5 秒后关闭，确认应用可启动。

## 风险或遗留项
- 本次已完成编译和启动验证，但未做人工像素级对照检查；若你继续给我新截图，我可以再按差异继续收紧细节。
