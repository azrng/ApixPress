# 继续压缩接口树层级占位

## 本次目标
- 继续缩小“用户目录”和下方接口项前方的层级空白。

## 核心改动
- 将自定义目录模板首列宽度从 `8` 压到 `6`。
- 将叶子节点前的 spacer 宽度压到 `0`，不再保留无意义占位。
- 将递归子节点容器左边距从 `4` 继续压到 `2`。

## 修改文件
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`
- `TASK.md`

## 校验情况
- `dotnet build -c Release`：通过
- `dotnet test`：通过（10/10）

## 风险或遗留项
- 当前已经把层级占位压得比较紧；如果还要继续左贴，下一步应考虑进一步缩小图标尺寸或用更窄的 chevron，而不是继续硬压缩层级列宽。
