# 继续修复接口点击闪退

## 本次目标

- 处理点击接口详情后右侧仍不展示并触发 `StackOverflow` 闪退的问题。
- 将请求代码弹框从请求编辑器高频切换区域移出，降低接口切换时的布局与绑定递归风险。

## 核心改动

- 新增 `RequestCodeDialogView`，把请求代码弹框接入主窗口统一 Overlay 层。
- 从 `RequestEditorWorkspaceView` 移除内联弹框视觉树，右侧请求编辑器只保留请求行与工作台内容。
- 移除请求行“生成代码”按钮上的 `CanGenerateRequestCode` 可用性绑定，由命令内部兜底判断。
- 在 `MainWindowViewModel` 增加 `ShowRequestCodeDialog`，统一驱动主窗口弹框显隐。
- 补充请求编辑器视图和请求代码弹框实例化 smoke test。
- 更新 `ui-AGENTS.md`，明确高频请求编辑区域不得内联挂载隐藏弹框。

## 修改文件

- `src/ApixPress.App/ViewModels/MainWindowViewModel.cs`
- `src/ApixPress.App/ViewModels/MainWindowViewModel.ShellState.cs`
- `src/ApixPress.App/Views/Controls/MainWindowOverlaysView.axaml`
- `src/ApixPress.App/Views/Controls/RequestEditorWorkspaceView.axaml`
- `src/ApixPress.App/Views/Controls/RequestCodeDialogView.axaml`
- `src/ApixPress.App/Views/Controls/RequestCodeDialogView.axaml.cs`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`
- `ui-AGENTS.md`
- `TASK.md`

## 校验情况

- 已执行 `dotnet test .\Apifox.slnx --no-restore`，通过 149 项测试。
- 已启动本地 Debug 桌面进程，应用可打开到首页；主窗口未再暴露隐藏的 `curl` 弹框可访问性节点。

## 风险或遗留项

- 本次未能自动定位并点击用户具体项目中的接口节点；已根据 Windows 事件日志确认此前崩溃为 `StackOverflow`，并移除了本轮新增的右侧内联隐藏弹框与新增可用性绑定。
