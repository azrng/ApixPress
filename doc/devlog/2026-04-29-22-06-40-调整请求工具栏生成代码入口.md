# 调整请求工具栏生成代码入口

## 本次目标

- 去掉请求编辑器顶部单独展示的发送预览。
- 将发送预览收进生成代码弹窗。
- 将“生成代码”按钮改成图标按钮，并移动到请求名称输入框后侧。

## 核心改动

- 调整请求编辑器顶部布局，快捷请求与 HTTP 接口两处都改为“名称输入框 + 代码图标按钮”的组合。
- 从 URL 工具栏中移除文字版“生成代码”按钮，并删除顶部单独的发送预览展示区域。
- 在生成代码弹窗中新增发送预览卡片，和 `curl` 代码一起展示。
- 为顶部名称输入框补充紧凑宽度样式，并新增代码图标按钮样式与弹窗预览卡片样式。

## 修改文件

- `src/ApixPress.App/Views/Controls/RequestEditorWorkspaceView.axaml`
- `src/ApixPress.App/Views/Controls/RequestCodeDialogView.axaml`
- `src/ApixPress.App/Assets/Styles/WorkspaceEditorStyles.axaml`
- `src/ApixPress.App/Assets/Styles/DialogStyles.axaml`
- `TASK.md`

## 校验情况

- 已执行整套测试：`dotnet test .\Apifox.slnx --no-restore`，通过 154 项测试。
- 已回读关键 XAML 片段，确认图标按钮已移动到名称输入框后侧，发送预览已从顶部移除并进入生成代码弹窗。

## 风险或遗留项

- 本次未执行人工界面 smoke test；当前结论基于 XAML 回读与自动化测试。
- 当前代码图标采用 `</>` 文本图形呈现，若后续需要与整站图标系统完全统一，可再替换为专用矢量图标资源。
