# 修复快捷请求粘贴后卡退

## 本次目标

- 修复快捷请求地址输入框粘贴内容后应用卡顿退出的问题。
- 让快捷请求粘贴完整 URL、curl 命令或带换行内容时保持稳定。

## 核心改动

- 在快捷请求地址写入 ViewModel 前规范化粘贴内容。
- 对包含 `http://` 或 `https://` 的多行文本、curl 命令等内容，提取第一个完整 URL。
- 对超长地址跳过同步发送预览解析，改为展示可理解的预览不可用提示，避免 UI 线程被长文本解析拖住。
- 补充快捷请求粘贴 curl 多行内容和超长 URL 预览降级的回归测试。

## 修改文件

- `src/ApixPress.App/ViewModels/ProjectRequestEditorViewModel.cs`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`
- `TASK.md`

## 校验情况

- 已执行 `dotnet test .\Apifox.slnx`，通过 145 项测试。

## 风险或遗留项

- 本次未启动桌面应用进行人工粘贴 smoke test；已通过 ViewModel 回归测试覆盖粘贴输入规范化和超长输入降级路径。
