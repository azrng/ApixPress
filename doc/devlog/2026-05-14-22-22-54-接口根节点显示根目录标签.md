# 接口根节点显示根目录标签

## 本次目标
- 点击左侧「接口」根节点时，顶部工作标签区显示并激活「根目录（默认模块）」标签。

## 核心改动
- 为工作标签增加 `interface-root` 类型。
- 打开接口根节点时创建或复用根目录标签，不再清空当前工作标签。
- 接口根工作区仍展示 Auth 与全部接口内容，具体接口打开时不会复用根目录标签。
- 补充 ViewModel 回归测试，覆盖根目录标签可见与激活状态。

## 修改文件
- `src/ApixPress.App/ViewModels/RequestWorkspaceTabViewModel.cs`
- `src/ApixPress.App/ViewModels/ProjectWorkspaceTabsViewModel.cs`
- `src/ApixPress.App/ViewModels/ProjectTabComposition.cs`
- `src/ApixPress.App/ViewModels/ProjectWorkspaceShellViewModel.cs`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`
- `TASK.md`

## 校验情况
- 已执行：`dotnet test tests/ApixPress.App.Tests/ApixPress.App.Tests.csproj`
- 已执行：`dotnet build ApixPress.slnx`

## 风险或遗留项
- 未执行真实窗口点击 smoke test；本次用 ViewModel 回归测试覆盖交互状态，并完成全量测试与构建。
