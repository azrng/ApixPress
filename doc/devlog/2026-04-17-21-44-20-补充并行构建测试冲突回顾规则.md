# 本次目标

- 对连续两次出现的 .NET 中间文件竞争问题做简短回顾，并补充仓库规则，避免后续再次并行触发伪失败。

# 核心改动

- 记录本次错误现象：并行执行 `dotnet build` 与 `dotnet test` 时，出现“当文件已存在时，无法创建该文件”的中间文件冲突。
- 归纳触发场景：同一项目同时写入共享的 `obj/bin` 目录。
- 明确根因：把本不适合并行的写操作命令放进了并行工具执行，导致 .NET 生成过程竞争同一批临时文件。
- 在根 `AGENTS.md` 中新增规则：`dotnet build/test/publish` 等共享中间目录的命令禁止并行执行。

# 修改文件

- `AGENTS.md`
- `doc/devlog/2026-04-17-21-44-20-JSON响应内容自动格式化展示.md`
- `TASK.md`

# 校验情况

- 已执行：规则文件与开发日志自查，确认中文与 UTF-8 正常
- 已执行：`dotnet test tests/ApixPress.App.Tests/ApixPress.App.Tests.csproj -c Release --no-build --filter "FullyQualifiedName~ResponseSectionViewModelTests"`

# 风险或遗留项

- 该规则目前以协作约束形式落地；若后续仍频繁发生类似冲突，可再考虑把验证命令串行化写入更明确的工作流脚本。

# 回顾分析

- 错误现象：测试在创建中间文件时失败，提示目标文件已存在。
- 触发场景：同一轮中并行启动 `dotnet build` 与 `dotnet test`。
- 根因：两个命令同时写共享的中间目录，产生文件竞争。
- 本次补充或修改的规则：禁止并行执行共享 `obj/bin` 目录的 `dotnet build/test/publish` 命令。
- 后续如何避免或验证不再复发：后续统一先 `build`，再在 `--no-build` 前提下顺序执行目标测试。
