# 收紧环境详情顶部编辑区

## 本次目标
- 压缩环境管理弹框中“环境名称”编辑区的高度，并移除左侧装饰性图标。

## 核心改动
- 将环境详情顶部区域从“图标 + 标签 + 输入框 + 说明”收成“标签 + 输入框”两层结构。
- 删除左侧装饰图标和底部说明文案，减少首屏垂直占用。
- 为 `EnvironmentDetailHeroCard` 补充更紧凑的内边距，并略收紧环境名称输入框高度。

## 修改文件
- `src/ApixPress.App/Views/Controls/EnvironmentManagerDialogView.axaml`
- `src/ApixPress.App/Assets/Styles/DialogStyles.axaml`
- `TASK.md`

## 校验情况
- 已执行 `dotnet msbuild src\\ApixPress.App\\Apifox.App.csproj /t:Compile /p:Configuration=Debug`，通过。

## 风险或遗留项
- 本次为环境管理弹框的局部布局微调，未额外补 UI 自动化测试。
- 若后续还觉得首屏偏高，可继续压缩区块间距或左侧环境列表项高度。
