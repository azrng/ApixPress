# 本次目标

- 推进第二批 Ursa 组件替换，优先处理请求历史、响应区状态徽标，以及参数区和环境变量区的删除按钮。

# 核心改动

- 将主窗口请求历史中的请求方法与响应状态，从手工 `Border + TextBlock` 改为 Ursa `Badge`。
- 将响应区顶部状态展示改为 Ursa `Badge`，并在 ViewModel 中补充状态到 Badge class 的映射逻辑。
- 将 Params / Body / Headers 的删除按钮改为 Ursa `IconButton`，统一使用内置删除图标。
- 将环境变量抽屉中的删除触发按钮改为 Ursa `IconButton`，与 `PopConfirm` 配合使用。

# 修改文件

- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/ViewModels/RequestHistoryItemViewModel.cs`
- `src/ApixPress.App/ViewModels/ResponseSectionViewModel.cs`
- `src/ApixPress.App/Views/Controls/EnvironmentDrawerView.axaml`
- `TASK.md`

# 校验情况

- 已执行 `dotnet build`，通过。
- 已执行 `dotnet test --no-build`，6/6 通过。
- 已执行 `dotnet publish src\ApixPress.App\Apifox.App.csproj -c Debug -o .artifacts\publish --no-build`，通过。
- 已执行发布产物启动验证，进程启动 8 秒后仍存活，随后手动结束。

# 风险或遗留项

- 本次主要是展示层替换，未新增专门的 UI 自动化测试。
- 旧的部分本地样式定义仍保留在全局样式文件中，当前不影响运行，后续可在独立整理任务中清理。
