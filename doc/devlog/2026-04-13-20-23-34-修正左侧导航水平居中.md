# 修正左侧导航水平居中

## 本次目标
- 修正项目工作区左侧导航在卡片内没有水平居中的问题。

## 核心改动
- 将左侧导航菜单外层容器设置为水平居中。
- 为 `ProjectWorkspaceNavMenu` 指定固定宽度并改为水平居中，避免菜单继续横向拉伸导致整组内容偏左。

## 修改文件
- `TASK.md`
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/Assets/Styles/Global.axaml`

## 校验情况
- 已执行 `dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 已执行 `dotnet test Apifox.slnx -c Release -p:UseAppHost=false --no-build`

## 风险或遗留项
- 本次只修正导航轨的横向对齐；如果后续还希望进一步压缩导航轨整体宽度，可以继续调整左侧第一列的固定列宽。
