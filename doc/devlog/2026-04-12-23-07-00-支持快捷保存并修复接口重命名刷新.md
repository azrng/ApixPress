# 本次目标

- 支持在请求编辑场景下通过 `Ctrl + S` 保存当前请求。
- 修复 HTTP 接口保存后标签名称更新，但左侧目录树仍显示旧名称的问题。

# 核心改动

- 在主窗口增加 `Ctrl + S` 快捷键监听，直接复用现有 `SaveCaseCommand` 保存当前激活请求。
- 修正 `request_cases` 的保存冲突键，改为基于主键 `id` 更新，确保重命名接口时持久化记录被正确覆盖。
- 补充仓储持久化测试，验证同一 `http-interface` 按原 `Id` 保存重命名后，重新读取只保留一条且名称为最新值。

# 修改文件

- `src/ApixPress.App/MainWindow.axaml.cs`
- `src/ApixPress.App/Repositories/Implementations/WorkspaceRepositories.cs`
- `tests/ApixPress.App.Tests/Repositories/WorkspacePersistenceTests.cs`
- `TASK.md`

# 校验情况

- 已执行：`dotnet test Apifox.slnx --filter WorkspacePersistenceTests -p:UseAppHost=false`
- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 已执行：`dotnet test Apifox.slnx -p:UseAppHost=false --no-build`
- 未执行：桌面端手工 smoke test，当前会话未直接启动应用窗口验证快捷键交互

# 风险或遗留项

- `Ctrl + S` 当前挂在窗口级 `KeyDown`，理论上会覆盖大部分编辑焦点场景，但仍建议在真实窗口里补一次文本框聚焦时的手工验证。
