# 开发记录

- 本次目标：将项目与发布版本改为按构建当天日期自动生成，格式统一为 `yyyy.M.d`。
- 核心改动：
  - 新增 `Directory.Build.props`，集中生成 `Version`、`InformationalVersion`、`AssemblyVersion`、`FileVersion`。
  - 调整 `.github/workflows/release.yml`，发布时不再读取 `VERSION`，改为按当天日期生成版本并注入主程序与升级器。
  - 更新设计文档说明，并补充日期版本格式的更新检查回归测试。
- 修改文件：
  - `Directory.Build.props`
  - `.github/workflows/release.yml`
  - `tests/ApixPress.App.Tests/Services/ApplicationUpdateServiceTests.cs`
  - `doc/design/设计文档.md`
  - `TASK.md`
- 校验情况：
  - 已执行 `dotnet msbuild src/ApixPress.App/Apifox.App.csproj -getProperty:Version -getProperty:InformationalVersion -getProperty:AssemblyVersion -getProperty:FileVersion -getProperty:IncludeSourceRevisionInInformationalVersion`
  - 已执行 `dotnet test tests/ApixPress.App.Tests/ApixPress.App.Tests.csproj -c Release`
  - 已执行 `dotnet publish src/ApixPress.App/Apifox.App.csproj -c Release -r win-x64 --self-contained true -o artifacts/verify/app`
  - 已执行 `dotnet publish src/ApixPress.Updater/ApixPress.Updater.csproj -c Release -r win-x64 --self-contained true -o artifacts/verify/updater`
  - 已确认 `ApixPress.exe` 与 `ApixPress.Updater.exe` 的 `ProductVersion` 为 `2026.4.21`
- 风险或遗留项：
  - 当前仓库中的旧 `VERSION` 文件未删除，避免触发未确认的删除操作；后续若确认可清理，再移除该遗留文件。
