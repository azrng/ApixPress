# 本次目标
- 修复项目详情页点击“项目设置”后右侧内容空白的问题。

# 核心改动
- 调整 `MainWindow.axaml` 中活动项目工作区的容器层级，给“接口管理/请求历史”增加独立包裹层，并把“项目设置”工作区从原编辑区层级中分离出来。
- 补齐请求编辑区与请求历史区缺失的 `Grid` 闭合，避免项目设置内容被错误嵌套到请求编辑区域内。
- 调整 `ShowProjectSettingsCommand`，保证一级导航切到项目设置时默认回到“基本设置”。
- 为该默认行为补充 ViewModel 测试。

# 修改文件
- `src/ApixPress.App/MainWindow.axaml`
- `src/ApixPress.App/ViewModels/ProjectTabViewModel.cs`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`
- `TASK.md`

# 校验情况
- 已执行：`dotnet build Apifox.slnx -c Release -p:UseAppHost=false`
- 已执行：`dotnet test Apifox.slnx -c Release -p:UseAppHost=false`
- 未执行：真实桌面 UI smoke test，当前会话未直接打开窗口做人工点击验证。

# 风险或遗留项
- 这次修复已经把项目设置从错误层级中拆出，但最终视觉效果仍建议下一轮结合真实界面再点一次“项目设置 / 导入数据”做人工确认。
