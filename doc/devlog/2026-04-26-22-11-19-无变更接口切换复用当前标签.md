# 无变更接口切换复用当前标签

## 本次目标
- 点击左侧接口列表切换接口时，若当前标签没有未保存变更，则直接复用当前标签打开新接口。
- 若当前标签存在未保存变更，则保留原标签并新开标签，避免覆盖编辑内容。

## 核心改动
- 为 `RequestWorkspaceTabViewModel` 增加干净状态基线和 `HasUnsavedChanges` 判断。
- 从左侧目录加载工作项时，优先复用已打开的同源标签；否则在当前标签干净且未固定时复用当前标签。
- 保存快捷请求、HTTP 接口和接口用例成功后刷新干净状态基线。
- 补充回归测试，覆盖干净标签复用和脏标签新开两条路径。

## 修改文件
- `src/ApixPress.App/ViewModels/RequestWorkspaceTabViewModel.cs`
- `src/ApixPress.App/ViewModels/ProjectWorkspaceTabsViewModel.cs`
- `src/ApixPress.App/ViewModels/ProjectWorkspaceCatalogViewModel.cs`
- `src/ApixPress.App/ViewModels/ProjectRequestWorkflowViewModel.cs`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`
- `TASK.md`

## 校验情况
- `git diff --check` 通过。
- `dotnet build .\src\ApixPress.App\Apifox.App.csproj -p:OutputPath=.artifacts\verify-app\` 通过。
- `dotnet test .\tests\ApixPress.App.Tests\ApixPress.App.Tests.csproj -p:OutDir="$PWD\.artifacts\verify-tests\"` 通过，116 个测试全部通过。

## 风险或遗留项
- 未做自动截图比对；本次主要为 ViewModel 状态流转，已通过单元测试覆盖关键路径。
