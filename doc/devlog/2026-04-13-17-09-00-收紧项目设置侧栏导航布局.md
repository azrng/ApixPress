# 收紧项目设置侧栏导航布局

## 本次变更
- 修正“项目设置”左侧区域因 Swagger 导入入口新增后出现的卡片堆叠问题。
- 将左侧从大卡片入口改为紧凑分组导航，保留“项目概览 / 数据管理 / 导入数据”层级。
- 将摘要信息收纳为底部小状态卡，避免挤压主内容区和破坏整体节奏。

## 核心实现
- 调整 `MainWindow.axaml` 中项目设置左侧结构，使用单一侧栏卡片承载分组标题和导航按钮。
- 更新 `Global.axaml`，补充项目设置侧栏紧凑导航与摘要卡样式。

## 验证
- 已执行 `dotnet build Apifox.slnx`

## 备注
- 本次主要修正布局，不涉及 Swagger 导入业务逻辑变更。
