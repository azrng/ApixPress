# 修复首页项目卡片间隙

## 本次目标
- 让首页项目列表卡片之间保留稳定横向和纵向间隙，避免内容贴在一起。

## 核心改动
- 调整首页项目列表 `WrapPanel` 的 `ItemWidth` 与 `ItemHeight`，让卡片右下间距在布局槽位中真实生效。

## 修改文件
- `src/ApixPress.App/Views/Controls/MainWindowHomeView.axaml`
- `TASK.md`

## 校验情况
- 已执行 `dotnet build src\ApixPress.App\Apifox.App.csproj -o artifacts\verify\ApixPress.App`，通过。
- 默认输出目录构建因当前运行中的 `ApixPress (37056)` 锁定 `bin\Debug\net10.0` 下的主程序文件而未完成。

## 风险或遗留项
- 未中断当前运行中的应用进程；新样式需在下次重启或重新运行后可见。
