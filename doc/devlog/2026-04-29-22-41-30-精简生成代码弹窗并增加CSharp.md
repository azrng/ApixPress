# 精简生成代码弹窗并增加 CSharp

## 本次目标

- 去掉生成代码弹窗中的发送预览区域。
- 在左侧语言列表中增加 `C#`。
- 为 `C#` 提供 `HttpClient` 实现代码。

## 核心改动

- 移除生成代码弹窗中的发送预览展示区，右侧内容区改为更紧凑的“标题 + 代码区域”结构。
- 将左侧语言栏从单一 `Shell` 扩展为 `Shell` 和 `C#` 两项，并增加对应切换命令与选中状态。
- 保留 `Shell` 下的 `curl / wget / powershell` 三个页签。
- 扩展请求代码生成器，新增 `C# HttpClient` 代码输出，覆盖请求方法、URL、请求头和常见 Body 模式。
- 更新回归测试，验证 `C#` 代码内容与语言切换状态。

## 修改文件

- `src/ApixPress.App/ViewModels/ProjectRequestEditorViewModel.cs`
- `src/ApixPress.App/ViewModels/ProjectHttpDocumentFormatter.cs`
- `src/ApixPress.App/Views/Controls/RequestCodeDialogView.axaml`
- `src/ApixPress.App/Assets/Styles/DialogStyles.axaml`
- `tests/ApixPress.App.Tests/ViewModels/ProjectTabViewModelTests.cs`
- `TASK.md`

## 校验情况

- 已执行整套测试：`dotnet test .\Apifox.slnx --no-restore`，通过 154 项测试。
- 已通过测试验证 `curl`、`wget`、`powershell` 和 `C# HttpClient` 代码生成及左侧语言切换状态。

## 风险或遗留项

- 本次未执行人工界面 smoke test；当前结论基于自动化测试与代码回读。
- 当前 `C#` 代码优先覆盖常见字符串 body、`x-www-form-urlencoded` 和基础 `form-data` 文本字段场景，若后续需要文件上传等更复杂内容，可再扩展。
